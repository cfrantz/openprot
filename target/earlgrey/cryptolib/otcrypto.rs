/* automatically generated by rust-bindgen 0.72.1 */

#![allow(non_camel_case_types)]
#![allow(non_upper_case_globals)]

#[doc = " OpenTitan's status_t is a single 32-bit word conveying either a result code\n or an error code.\n - The otcrypto has only one result code: The `Ok` value which is\n   equivalent in value to kHardenedBoolTrue.\n - The otcrypto error codes all have the MSB set and encode an error type of\n   absl_status_t in the lower 5 bits.\n\n   This definition is supplied to provide the status_t definition when\n   otcrypto is exported out of the OpenTitan repository."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct status {
    pub value: i32,
}
#[doc = " OpenTitan's status_t is a single 32-bit word conveying either a result code\n or an error code.\n - The otcrypto has only one result code: The `Ok` value which is\n   equivalent in value to kHardenedBoolTrue.\n - The otcrypto error codes all have the MSB set and encode an error type of\n   absl_status_t in the lower 5 bits.\n\n   This definition is supplied to provide the status_t definition when\n   otcrypto is exported out of the OpenTitan repository."]
pub type status_t = status;
#[doc = " The truthy value, expected to be used like #true."]
pub const hardened_bool_kHardenedBoolTrue: hardened_bool = 1849;
#[doc = " The falsey value, expected to be used like #false."]
pub const hardened_bool_kHardenedBoolFalse: hardened_bool = 468;
#[doc = " This is a boolean type for use in hardened contexts.\n\n The intention is that this is used instead of `<stdbool.h>`'s #bool, where a\n higher hamming distance is required between the truthy and the falsey value.\n\n The values below were chosen at random, with some specific restrictions. They\n have a Hamming Distance of 8, and they are 11-bit values so they can be\n materialized with a single instruction on RISC-V. They are also specifically\n not the complement of each other."]
pub type hardened_bool = ::core::ffi::c_uint;
#[doc = " This is a boolean type for use in hardened contexts.\n\n The intention is that this is used instead of `<stdbool.h>`'s #bool, where a\n higher hamming distance is required between the truthy and the falsey value.\n\n The values below were chosen at random, with some specific restrictions. They\n have a Hamming Distance of 8, and they are 11-bit values so they can be\n materialized with a single instruction on RISC-V. They are also specifically\n not the complement of each other."]
pub use self::hardened_bool as hardened_bool_t;
#[doc = " Return values for the crypto library.\n\n The crypto library's return value is defined as OpenTitan's internal\n `status_t` in order to simplify testing. However, informally the library\n guarantees that the concrete value contained in the status will be one of\n the members of the `otcrypto_status_value` enum below."]
pub type otcrypto_status_t = status_t;
pub const otcrypto_status_value_kOtcryptoStatusValueOk: otcrypto_status_value = 1849;
pub const otcrypto_status_value_kOtcryptoStatusValueBadArgs: otcrypto_status_value = -2147418461;
pub const otcrypto_status_value_kOtcryptoStatusValueInternalError: otcrypto_status_value =
    -2147462326;
pub const otcrypto_status_value_kOtcryptoStatusValueFatalError: otcrypto_status_value = -2147455607;
pub const otcrypto_status_value_kOtcryptoStatusValueAsyncIncomplete: otcrypto_status_value =
    -2147423666;
pub const otcrypto_status_value_kOtcryptoStatusValueNotImplemented: otcrypto_status_value =
    -2147447508;
#[doc = " Possible status values for the cryptolib.\n\n As long as the OTCRYPTO_STATUS_DEBUG define is unset, all `otcrypto_status_t`\n codes returned by the cryptolib should be bit-by-bit equivalent with one of\n the values in this enum.\n\n Values are built to be bit-compatible with OpenTitan's internal `status_t`\n datatypes. The highest (sign) bit indicates if the value is an error (1) or\n not (0). For non-error statuses, the rest can be anything; in cryptolib\n status codes it is always `kHardenedBoolTrue`. For errors:\n   - The next 15 bits are a module identifier, which is always 0 in the\n     cryptolib status codes\n   - The next 11 bits are a line number or other information; in the\n     cryptolib status codes, it is a hardened value created to have high\n     Hamming distance with the other valid status codes\n   - The final 5 bits are an Abseil-compatible error code\n\n The hardened values for error codes were generated with:\n $ ./util/design/sparse-fsm-encode.py -d 5 -m 5 -n 11 \\\n      -s 4232058530 --language=sv --avoid-zero\n\n Use the same seed value and a larger `-m` argument to generate new values\n without changing all error codes. Remove the seed (-s argument) to generate\n completely new 11-bit values."]
pub type otcrypto_status_value = ::core::ffi::c_int;
#[doc = " Possible status values for the cryptolib.\n\n As long as the OTCRYPTO_STATUS_DEBUG define is unset, all `otcrypto_status_t`\n codes returned by the cryptolib should be bit-by-bit equivalent with one of\n the values in this enum.\n\n Values are built to be bit-compatible with OpenTitan's internal `status_t`\n datatypes. The highest (sign) bit indicates if the value is an error (1) or\n not (0). For non-error statuses, the rest can be anything; in cryptolib\n status codes it is always `kHardenedBoolTrue`. For errors:\n   - The next 15 bits are a module identifier, which is always 0 in the\n     cryptolib status codes\n   - The next 11 bits are a line number or other information; in the\n     cryptolib status codes, it is a hardened value created to have high\n     Hamming distance with the other valid status codes\n   - The final 5 bits are an Abseil-compatible error code\n\n The hardened values for error codes were generated with:\n $ ./util/design/sparse-fsm-encode.py -d 5 -m 5 -n 11 \\\n      -s 4232058530 --language=sv --avoid-zero\n\n Use the same seed value and a larger `-m` argument to generate new values\n without changing all error codes. Remove the seed (-s argument) to generate\n completely new 11-bit values."]
pub use self::otcrypto_status_value as otcrypto_status_value_t;
#[doc = " Struct to hold a fixed-length byte array.\n\n Note: the caller must (1) allocate sufficient space and (2) set the `len`\n field and `data` pointer when `otcrypto_byte_buf_t` is used for output. The\n crypto library will throw an error if `len` doesn't match expectations."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct otcrypto_byte_buf {
    pub data: *mut u8,
    pub len: usize,
}
#[doc = " Struct to hold a fixed-length byte array.\n\n Note: the caller must (1) allocate sufficient space and (2) set the `len`\n field and `data` pointer when `otcrypto_byte_buf_t` is used for output. The\n crypto library will throw an error if `len` doesn't match expectations."]
pub type otcrypto_byte_buf_t = otcrypto_byte_buf;
#[doc = " Struct to hold a constant fixed-length byte array.\n\n The const annotations prevent any changes to the byte buffer. It is\n necessary to have this structure separate from `otcrypto_byte_buf_t` because\n data pointed to by a struct does not inherit `const`, so `const\n otcrypto_byte_buf_t` would still allow data to change."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct otcrypto_const_byte_buf {
    pub data: *const u8,
    pub len: usize,
}
#[doc = " Struct to hold a constant fixed-length byte array.\n\n The const annotations prevent any changes to the byte buffer. It is\n necessary to have this structure separate from `otcrypto_byte_buf_t` because\n data pointed to by a struct does not inherit `const`, so `const\n otcrypto_byte_buf_t` would still allow data to change."]
pub type otcrypto_const_byte_buf_t = otcrypto_const_byte_buf;
#[doc = " Struct to hold a fixed-length word array.\n\n Note: the caller must (1) allocate sufficient space and (2) set the `len`\n field and `data` pointer when `otcrypto_word32_buf_t` is used for output. The\n crypto library will throw an error if `len` doesn't match expectations."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct otcrypto_word32_buf {
    pub data: *mut u32,
    pub len: usize,
}
#[doc = " Struct to hold a fixed-length word array.\n\n Note: the caller must (1) allocate sufficient space and (2) set the `len`\n field and `data` pointer when `otcrypto_word32_buf_t` is used for output. The\n crypto library will throw an error if `len` doesn't match expectations."]
pub type otcrypto_word32_buf_t = otcrypto_word32_buf;
#[doc = " Struct to hold a constant fixed-length word array.\n\n The const annotations prevent any changes to the word buffer. It is\n necessary to have this structure separate from `otcrypto_word32_buf_t`\n because data pointed to by a struct does not inherit `const`, so `const\n otcrypto_word32_buf_t` would still allow data to change."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct otcrypto_const_word32_buf {
    pub data: *const u32,
    pub len: usize,
}
#[doc = " Struct to hold a constant fixed-length word array.\n\n The const annotations prevent any changes to the word buffer. It is\n necessary to have this structure separate from `otcrypto_word32_buf_t`\n because data pointed to by a struct does not inherit `const`, so `const\n otcrypto_word32_buf_t` would still allow data to change."]
pub type otcrypto_const_word32_buf_t = otcrypto_const_word32_buf;
pub const otcrypto_key_type_kOtcryptoKeyTypeAes: otcrypto_key_type = 2281;
pub const otcrypto_key_type_kOtcryptoKeyTypeHmac: otcrypto_key_type = 3647;
pub const otcrypto_key_type_kOtcryptoKeyTypeKmac: otcrypto_key_type = 2932;
pub const otcrypto_key_type_kOtcryptoKeyTypeRsa: otcrypto_key_type = 2030;
pub const otcrypto_key_type_kOtcryptoKeyTypeEcc: otcrypto_key_type = 347;
pub const otcrypto_key_type_kOtcryptoKeyTypeKdf: otcrypto_key_type = 2951;
#[doc = " Enum to denote the key type of the handled key.\n\n Values are hardened."]
pub type otcrypto_key_type = ::core::ffi::c_uint;
#[doc = " Enum to denote the key type of the handled key.\n\n Values are hardened."]
pub use self::otcrypto_key_type as otcrypto_key_type_t;
pub const otcrypto_aes_key_mode_kOtcryptoAesKeyModeEcb: otcrypto_aes_key_mode = 438;
pub const otcrypto_aes_key_mode_kOtcryptoAesKeyModeCbc: otcrypto_aes_key_mode = 3898;
pub const otcrypto_aes_key_mode_kOtcryptoAesKeyModeCfb: otcrypto_aes_key_mode = 249;
pub const otcrypto_aes_key_mode_kOtcryptoAesKeyModeOfb: otcrypto_aes_key_mode = 2889;
pub const otcrypto_aes_key_mode_kOtcryptoAesKeyModeCtr: otcrypto_aes_key_mode = 1230;
pub const otcrypto_aes_key_mode_kOtcryptoAesKeyModeGcm: otcrypto_aes_key_mode = 2725;
pub const otcrypto_aes_key_mode_kOtcryptoAesKeyModeKwp: otcrypto_aes_key_mode = 2005;
#[doc = " Enum to specify the AES modes that use a key.\n\n This will be used in the `otcrypto_key_mode_t` struct to indicate the mode\n for which the provided key is intended for.\n\n Values are hardened."]
pub type otcrypto_aes_key_mode = ::core::ffi::c_uint;
#[doc = " Enum to specify the AES modes that use a key.\n\n This will be used in the `otcrypto_key_mode_t` struct to indicate the mode\n for which the provided key is intended for.\n\n Values are hardened."]
pub use self::otcrypto_aes_key_mode as otcrypto_aes_key_mode_t;
pub const otcrypto_hmac_key_mode_kOtcryptoHmacKeyModeSha256: otcrypto_hmac_key_mode = 2045;
pub const otcrypto_hmac_key_mode_kOtcryptoHmacKeyModeSha384: otcrypto_hmac_key_mode = 1083;
pub const otcrypto_hmac_key_mode_kOtcryptoHmacKeyModeSha512: otcrypto_hmac_key_mode = 1954;
#[doc = " Enum to specify the HMAC modes that use a key.\n\n This will be used in the `otcrypto_key_mode_t` struct to indicate the mode\n for which the provided key is intended for.\n\n Values are hardened."]
pub type otcrypto_hmac_key_mode = ::core::ffi::c_uint;
#[doc = " Enum to specify the HMAC modes that use a key.\n\n This will be used in the `otcrypto_key_mode_t` struct to indicate the mode\n for which the provided key is intended for.\n\n Values are hardened."]
pub use self::otcrypto_hmac_key_mode as otcrypto_hmac_key_mode_t;
pub const otcrypto_kmac_key_mode_kOtcryptoKmacKeyModeKmac128: otcrypto_kmac_key_mode = 2646;
pub const otcrypto_kmac_key_mode_kOtcryptoKmacKeyModeKmac256: otcrypto_kmac_key_mode = 1635;
#[doc = " Enum to specify the KMAC modes that use a key.\n\n This will be used in the `otcrypto_key_mode_t` struct to indicate the mode\n for which the provided key is intended for.\n\n Values are hardened."]
pub type otcrypto_kmac_key_mode = ::core::ffi::c_uint;
#[doc = " Enum to specify the KMAC modes that use a key.\n\n This will be used in the `otcrypto_key_mode_t` struct to indicate the mode\n for which the provided key is intended for.\n\n Values are hardened."]
pub use self::otcrypto_kmac_key_mode as otcrypto_kmac_key_mode_t;
pub const otcrypto_rsa_key_mode_kOtcryptoRsaKeyModeSignPkcs: otcrypto_rsa_key_mode = 980;
pub const otcrypto_rsa_key_mode_kOtcryptoRsaKeyModeSignPss: otcrypto_rsa_key_mode = 1889;
pub const otcrypto_rsa_key_mode_kOtcryptoRsaKeyModeEncryptOaep: otcrypto_rsa_key_mode = 1413;
#[doc = " Enum to specify the RSA modes that use a key.\n\n This will be used in the `otcrypto_key_mode_t` struct to indicate the mode\n for which the provided key is intended for.\n\n Values are hardened."]
pub type otcrypto_rsa_key_mode = ::core::ffi::c_uint;
#[doc = " Enum to specify the RSA modes that use a key.\n\n This will be used in the `otcrypto_key_mode_t` struct to indicate the mode\n for which the provided key is intended for.\n\n Values are hardened."]
pub use self::otcrypto_rsa_key_mode as otcrypto_rsa_key_mode_t;
pub const otcrypto_ecc_key_mode_kOtcryptoEccKeyModeEcdsaP256: otcrypto_ecc_key_mode = 798;
pub const otcrypto_ecc_key_mode_kOtcryptoEccKeyModeEcdsaP384: otcrypto_ecc_key_mode = 1685;
pub const otcrypto_ecc_key_mode_kOtcryptoEccKeyModeEcdhP256: otcrypto_ecc_key_mode = 1532;
pub const otcrypto_ecc_key_mode_kOtcryptoEccKeyModeEcdhP384: otcrypto_ecc_key_mode = 455;
pub const otcrypto_ecc_key_mode_kOtcryptoEccKeyModeEd25519: otcrypto_ecc_key_mode = 1635;
pub const otcrypto_ecc_key_mode_kOtcryptoEccKeyModeX25519: otcrypto_ecc_key_mode = 187;
#[doc = " Enum to specify the ECC modes that use a key.\n\n This will be used in the `otcrypto_key_mode_t` struct to indicate the mode\n for which the provided key is intended for.\n\n Values are hardened."]
pub type otcrypto_ecc_key_mode = ::core::ffi::c_uint;
#[doc = " Enum to specify the ECC modes that use a key.\n\n This will be used in the `otcrypto_key_mode_t` struct to indicate the mode\n for which the provided key is intended for.\n\n Values are hardened."]
pub use self::otcrypto_ecc_key_mode as otcrypto_ecc_key_mode_t;
pub const otcrypto_kdf_key_mode_kOtcryptoKdfKeyModeCtrHmac: otcrypto_kdf_key_mode = 303;
pub const otcrypto_kdf_key_mode_kOtcryptoKdfKeyModeKmac128: otcrypto_kdf_key_mode = 3678;
pub const otcrypto_kdf_key_mode_kOtcryptoKdfKeyModeKmac256: otcrypto_kdf_key_mode = 851;
#[doc = " Enum to specify the KDF modes that use a key.\n\n This will be used in the `otcrypto_key_mode_t` struct to indicate the mode\n for which the provided key is intended for.\n\n Values are hardened."]
pub type otcrypto_kdf_key_mode = ::core::ffi::c_uint;
#[doc = " Enum to specify the KDF modes that use a key.\n\n This will be used in the `otcrypto_key_mode_t` struct to indicate the mode\n for which the provided key is intended for.\n\n Values are hardened."]
pub use self::otcrypto_kdf_key_mode as otcrypto_kdf_key_mode_t;
pub const otcrypto_key_mode_kOtcryptoKeyModeAesEcb: otcrypto_key_mode = 149488054;
pub const otcrypto_key_mode_kOtcryptoKeyModeAesCbc: otcrypto_key_mode = 149491514;
pub const otcrypto_key_mode_kOtcryptoKeyModeAesCfb: otcrypto_key_mode = 149487865;
pub const otcrypto_key_mode_kOtcryptoKeyModeAesOfb: otcrypto_key_mode = 149490505;
pub const otcrypto_key_mode_kOtcryptoKeyModeAesCtr: otcrypto_key_mode = 149488846;
pub const otcrypto_key_mode_kOtcryptoKeyModeAesGcm: otcrypto_key_mode = 149490341;
pub const otcrypto_key_mode_kOtcryptoKeyModeAesKwp: otcrypto_key_mode = 149489621;
pub const otcrypto_key_mode_kOtcryptoKeyModeHmacSha256: otcrypto_key_mode = 239011837;
pub const otcrypto_key_mode_kOtcryptoKeyModeHmacSha384: otcrypto_key_mode = 239010875;
pub const otcrypto_key_mode_kOtcryptoKeyModeHmacSha512: otcrypto_key_mode = 239011746;
pub const otcrypto_key_mode_kOtcryptoKeyModeKmac128: otcrypto_key_mode = 192154198;
pub const otcrypto_key_mode_kOtcryptoKeyModeKmac256: otcrypto_key_mode = 192153187;
pub const otcrypto_key_mode_kOtcryptoKeyModeRsaSignPkcs: otcrypto_key_mode = 133039060;
pub const otcrypto_key_mode_kOtcryptoKeyModeRsaSignPss: otcrypto_key_mode = 133039969;
pub const otcrypto_key_mode_kOtcryptoKeyModeRsaEncryptOaep: otcrypto_key_mode = 133039493;
pub const otcrypto_key_mode_kOtcryptoKeyModeEcdsaP256: otcrypto_key_mode = 22741790;
pub const otcrypto_key_mode_kOtcryptoKeyModeEcdsaP384: otcrypto_key_mode = 22742677;
pub const otcrypto_key_mode_kOtcryptoKeyModeEcdhP256: otcrypto_key_mode = 22742524;
pub const otcrypto_key_mode_kOtcryptoKeyModeEcdhP384: otcrypto_key_mode = 22741447;
pub const otcrypto_key_mode_kOtcryptoKeyModeEd25519: otcrypto_key_mode = 22742627;
pub const otcrypto_key_mode_kOtcryptoKeyModeX25519: otcrypto_key_mode = 22741179;
pub const otcrypto_key_mode_kOtcryptoKeyModeKdfCtrHmac: otcrypto_key_mode = 193397039;
pub const otcrypto_key_mode_kOtcryptoKeyModeKdfKmac128: otcrypto_key_mode = 193400414;
pub const otcrypto_key_mode_kOtcryptoKeyModeKdfKmac256: otcrypto_key_mode = 193397587;
#[doc = " Enum for opentitan crypto modes that use a key.\n\n Denotes the crypto mode for which the provided key is to be used.\n This `otcrypto_key_mode_t` will be a parameter in the\n `otcrypto_blinded_key_t` and `otcrypto_unblinded_key_t` structs.\n\n Values are hardened."]
pub type otcrypto_key_mode = ::core::ffi::c_uint;
#[doc = " Enum for opentitan crypto modes that use a key.\n\n Denotes the crypto mode for which the provided key is to be used.\n This `otcrypto_key_mode_t` will be a parameter in the\n `otcrypto_blinded_key_t` and `otcrypto_unblinded_key_t` structs.\n\n Values are hardened."]
pub use self::otcrypto_key_mode as otcrypto_key_mode_t;
pub const otcrypto_key_security_level_kOtcryptoKeySecurityLevelLow: otcrypto_key_security_level =
    489;
pub const otcrypto_key_security_level_kOtcryptoKeySecurityLevelMedium: otcrypto_key_security_level =
    3755;
pub const otcrypto_key_security_level_kOtcryptoKeySecurityLevelHigh: otcrypto_key_security_level =
    2686;
#[doc = " Enum to denote key security level.\n\n At high security levels, the crypto library will prioritize\n protecting the key from sophisticated attacks, even at large\n performance costs. If the security level is low, the crypto\n library will still try to protect the key, but may forgo the\n most costly protections against e.g. sophisticated physical\n attacks.\n\n Values are hardened."]
pub type otcrypto_key_security_level = ::core::ffi::c_uint;
#[doc = " Enum to denote key security level.\n\n At high security levels, the crypto library will prioritize\n protecting the key from sophisticated attacks, even at large\n performance costs. If the security level is low, the crypto\n library will still try to protect the key, but may forgo the\n most costly protections against e.g. sophisticated physical\n attacks.\n\n Values are hardened."]
pub use self::otcrypto_key_security_level as otcrypto_key_security_level_t;
pub const otcrypto_lib_version_kOtcryptoLibVersion1: otcrypto_lib_version = 2036;
#[doc = " Enum to denote the crypto library version.\n\n In future updates, this enum will be extended to preserve some\n level of backwards-compatibility despite changes to internal\n details (for example, the preferred masking scheme for blinded\n keys).\n\n Values are hardened."]
pub type otcrypto_lib_version = ::core::ffi::c_uint;
#[doc = " Enum to denote the crypto library version.\n\n In future updates, this enum will be extended to preserve some\n level of backwards-compatibility despite changes to internal\n details (for example, the preferred masking scheme for blinded\n keys).\n\n Values are hardened."]
pub use self::otcrypto_lib_version as otcrypto_lib_version_t;
#[doc = " Struct to represent the configuration of a blinded key."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct otcrypto_key_config {
    pub version: otcrypto_lib_version_t,
    pub key_mode: otcrypto_key_mode_t,
    pub key_length: u32,
    pub hw_backed: hardened_bool_t,
    pub exportable: hardened_bool_t,
    pub security_level: otcrypto_key_security_level_t,
}
#[doc = " Struct to represent the configuration of a blinded key."]
pub type otcrypto_key_config_t = otcrypto_key_config;
#[doc = " Struct to handle unmasked key type."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct otcrypto_unblinded_key {
    pub key_mode: otcrypto_key_mode_t,
    pub key_length: u32,
    pub key: *mut u32,
    pub checksum: u32,
}
#[doc = " Struct to handle unmasked key type."]
pub type otcrypto_unblinded_key_t = otcrypto_unblinded_key;
#[doc = " Struct to handle masked key type."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct otcrypto_blinded_key {
    pub config: otcrypto_key_config_t,
    pub keyblob_length: u32,
    pub keyblob: *mut u32,
    pub checksum: u32,
}
#[doc = " Struct to handle masked key type."]
pub type otcrypto_blinded_key_t = otcrypto_blinded_key;
pub const otcrypto_hash_mode_kOtcryptoHashModeSha256: otcrypto_hash_mode = 1691;
pub const otcrypto_hash_mode_kOtcryptoHashModeSha384: otcrypto_hash_mode = 1966;
pub const otcrypto_hash_mode_kOtcryptoHashModeSha512: otcrypto_hash_mode = 369;
pub const otcrypto_hash_mode_kOtcryptoHashModeSha3_224: otcrypto_hash_mode = 1302;
pub const otcrypto_hash_mode_kOtcryptoHashModeSha3_256: otcrypto_hash_mode = 724;
pub const otcrypto_hash_mode_kOtcryptoHashModeSha3_384: otcrypto_hash_mode = 615;
pub const otcrypto_hash_mode_kOtcryptoHashModeSha3_512: otcrypto_hash_mode = 1101;
pub const otcrypto_hash_mode_kOtcryptoHashXofModeShake128: otcrypto_hash_mode = 1496;
pub const otcrypto_hash_mode_kOtcryptoHashXofModeShake256: otcrypto_hash_mode = 842;
pub const otcrypto_hash_mode_kOtcryptoHashXofModeCshake128: otcrypto_hash_mode = 189;
pub const otcrypto_hash_mode_kOtcryptoHashXofModeCshake256: otcrypto_hash_mode = 1250;
#[doc = " Enum to define supported hashing modes.\n\n Values are hardened."]
pub type otcrypto_hash_mode = ::core::ffi::c_uint;
#[doc = " Enum to define supported hashing modes.\n\n Values are hardened."]
pub use self::otcrypto_hash_mode as otcrypto_hash_mode_t;
#[doc = " Container for a hash digest."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct otcrypto_hash_digest {
    pub mode: otcrypto_hash_mode_t,
    pub data: *mut u32,
    pub len: usize,
}
#[doc = " Container for a hash digest."]
pub type otcrypto_hash_digest_t = otcrypto_hash_digest;
pub const otcrypto_aes_mode_kOtcryptoAesModeEcb: otcrypto_aes_mode = 1331;
pub const otcrypto_aes_mode_kOtcryptoAesModeCbc: otcrypto_aes_mode = 1117;
pub const otcrypto_aes_mode_kOtcryptoAesModeCfb: otcrypto_aes_mode = 3282;
pub const otcrypto_aes_mode_kOtcryptoAesModeOfb: otcrypto_aes_mode = 922;
pub const otcrypto_aes_mode_kOtcryptoAesModeCtr: otcrypto_aes_mode = 3372;
#[doc = " Enum to define AES mode of operation.\n\n Values are hardened."]
pub type otcrypto_aes_mode = ::core::ffi::c_uint;
#[doc = " Enum to define AES mode of operation.\n\n Values are hardened."]
pub use self::otcrypto_aes_mode as otcrypto_aes_mode_t;
pub const otcrypto_aes_operation_kOtcryptoAesOperationEncrypt: otcrypto_aes_operation = 694;
pub const otcrypto_aes_operation_kOtcryptoAesOperationDecrypt: otcrypto_aes_operation = 1520;
#[doc = " Enum to define AES operation to be performed.\n\n Values are hardened."]
pub type otcrypto_aes_operation = ::core::ffi::c_uint;
#[doc = " Enum to define AES operation to be performed.\n\n Values are hardened."]
pub use self::otcrypto_aes_operation as otcrypto_aes_operation_t;
pub const otcrypto_aes_padding_kOtcryptoAesPaddingPkcs7: otcrypto_aes_padding = 3711;
pub const otcrypto_aes_padding_kOtcryptoAesPaddingIso9797M2: otcrypto_aes_padding = 4012;
pub const otcrypto_aes_padding_kOtcryptoAesPaddingNull: otcrypto_aes_padding = 2254;
#[doc = " Enum to define padding scheme for AES data.\n\n Values are hardened."]
pub type otcrypto_aes_padding = ::core::ffi::c_uint;
#[doc = " Enum to define padding scheme for AES data.\n\n Values are hardened."]
pub use self::otcrypto_aes_padding as otcrypto_aes_padding_t;
unsafe extern "C" {
    #[doc = " Get the number of blocks needed for the plaintext length and padding mode.\n\n This returns the size of the padded plaintext, which is the same as the\n ciphertext size. Returns `kOtcryptoStatusValueBadArgs` if the padding mode\n and length are incompatible (for instance, if the padding mode is \"no\n padding\" but the input length is not a multiple of the AES block size).\n\n @param plaintext_len Plaintext data length in bytes.\n @param aes_padding Padding scheme to be used for the data.\n @return Size of the padded input or ciphertext.\n @return Result of the operation."]
    pub fn otcrypto_aes_padded_plaintext_length(
        plaintext_len: usize,
        aes_padding: otcrypto_aes_padding_t,
        padded_len: *mut usize,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Performs the AES operation.\n\n The input data in the `cipher_input` is first padded using the\n `aes_padding` scheme and the output is copied to `cipher_output`.\n\n The caller should allocate space for the `cipher_output` buffer, which is\n given in bytes by `otcrypto_aes_padded_plaintext_length`, and set the number\n of bytes allocated in the `len` field of the output.  If the user-set length\n and the expected length do not match, an error message will be returned.\n\n Note that, during decryption, the padding mode is ignored. This function\n will NOT check the padding or return an error if the padding is invalid,\n since doing so could expose a padding oracle (especially in CBC mode).\n\n @param key Pointer to the blinded key struct with key shares.\n @param iv Initialization vector, used for CBC, CFB, OFB, CTR modes. May be\n           NULL if mode is ECB.\n @param aes_mode Required AES mode of operation.\n @param aes_operation Required AES operation (encrypt or decrypt).\n @param cipher_input Input data to be ciphered.\n @param aes_padding Padding scheme to be used for the data.\n @param[out] cipher_output Output data after cipher operation.\n @return The result of the cipher operation."]
    pub fn otcrypto_aes(
        key: *mut otcrypto_blinded_key_t,
        iv: otcrypto_word32_buf_t,
        aes_mode: otcrypto_aes_mode_t,
        aes_operation: otcrypto_aes_operation_t,
        cipher_input: otcrypto_const_byte_buf_t,
        aes_padding: otcrypto_aes_padding_t,
        cipher_output: otcrypto_byte_buf_t,
    ) -> otcrypto_status_t;
}
pub const otcrypto_aes_gcm_tag_len_kOtcryptoAesGcmTagLen128: otcrypto_aes_gcm_tag_len = 359;
pub const otcrypto_aes_gcm_tag_len_kOtcryptoAesGcmTagLen96: otcrypto_aes_gcm_tag_len = 858;
pub const otcrypto_aes_gcm_tag_len_kOtcryptoAesGcmTagLen64: otcrypto_aes_gcm_tag_len = 1492;
pub const otcrypto_aes_gcm_tag_len_kOtcryptoAesGcmTagLen32: otcrypto_aes_gcm_tag_len = 3846;
#[doc = " Enum to denote the AES-GCM tag length.\n\n Values are hardened."]
pub type otcrypto_aes_gcm_tag_len = ::core::ffi::c_uint;
#[doc = " Enum to denote the AES-GCM tag length.\n\n Values are hardened."]
pub use self::otcrypto_aes_gcm_tag_len as otcrypto_aes_gcm_tag_len_t;
#[doc = " Context for a streaming AES-GCM operation.\n\n Representation is internal to the AES-GCM implementation and subject to\n change."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct otcrypto_aes_gcm_context {
    pub data: [u32; 194usize],
}
#[doc = " Context for a streaming AES-GCM operation.\n\n Representation is internal to the AES-GCM implementation and subject to\n change."]
pub type otcrypto_aes_gcm_context_t = otcrypto_aes_gcm_context;
unsafe extern "C" {
    #[doc = " Performs the AES-GCM authenticated encryption operation.\n\n This function encrypts the input `plaintext` to produce an\n output `ciphertext`. Together it generates an authentication\n tag `auth_tag` on the ciphered data and any non-confidential\n additional authenticated data `aad`.\n\n The caller should allocate space for the `ciphertext` buffer,\n (same length as input), `auth_tag` buffer (same as tag_len), and\n set the length of expected outputs in the `len` field of\n `ciphertext` and `auth_tag`. If the user-set length and the output\n length does not match, an error message will be returned.\n\n @param key Pointer to the blinded gcm-key struct.\n @param plaintext Input data to be encrypted and authenticated.\n @param iv Initialization vector for the encryption function.\n @param aad Additional authenticated data.\n @param tag_len Length of authentication tag to be generated.\n @param[out] ciphertext Encrypted output data, same length as input data.\n @param[out] auth_tag Generated authentication tag.\n @return Result of the authenticated encryption.\n operation"]
    pub fn otcrypto_aes_gcm_encrypt(
        key: *mut otcrypto_blinded_key_t,
        plaintext: otcrypto_const_byte_buf_t,
        iv: otcrypto_const_word32_buf_t,
        aad: otcrypto_const_byte_buf_t,
        tag_len: otcrypto_aes_gcm_tag_len_t,
        ciphertext: otcrypto_byte_buf_t,
        auth_tag: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Performs the AES-GCM authenticated decryption operation.\n\n This function first verifies if the authentication tag `auth_tag`\n matches the internally generated tag. Upon verification, the\n function decrypts the input `ciphertext` to get a `plaintext data.\n\n The caller should allocate space for the `plaintext` buffer,\n (same length as ciphertext), and set the length of expected output\n in the `len` field of `plaintext`. If the user-set length and the\n output length does not match, an error message will be returned.\n\n The caller must check the `success` argument before operating on\n `plaintext`. If the authentication check fails, then `plaintext` should not\n be used and there are no guarantees about its contents.\n\n @param key Pointer to the blinded gcm-key struct.\n @param ciphertext Input data to be decrypted.\n @param iv Initialization vector for the decryption function.\n @param aad Additional authenticated data.\n @param tag_len Length of authentication tag to be generated.\n @param auth_tag Authentication tag to be verified.\n @param[out] plaintext Decrypted plaintext data, same len as input data.\n @param[out] success True if the authentication check passed, otherwise false.\n @return Result of the authenticated decryption.\n operation"]
    pub fn otcrypto_aes_gcm_decrypt(
        key: *mut otcrypto_blinded_key_t,
        ciphertext: otcrypto_const_byte_buf_t,
        iv: otcrypto_const_word32_buf_t,
        aad: otcrypto_const_byte_buf_t,
        tag_len: otcrypto_aes_gcm_tag_len_t,
        auth_tag: otcrypto_const_word32_buf_t,
        plaintext: otcrypto_byte_buf_t,
        success: *mut hardened_bool_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Initializes the AES-GCM authenticated encryption operation.\n\n The order of operations for encryption is:\n   - `otcrypto_aes_gcm_encrypt_init()` called once\n   - `otcrypto_aes_gcm_update_aad()` called zero or more times\n   - `otcrypto_aes_gcm_update_encrypted_data()` called zero or more times\n   - `otcrypto_aes_gcm_encrypt_final()` called once\n\n Associated data must be added first, before encrypted data; the caller may\n not call `otcrypto_aes_gcm_udpate_aad()` after the first call to\n `otcrypto_aes_gcm_update_encrypted_data()`.\n\n The resulting AES-GCM context will include pointers into the keyblob of the\n blinded key. It is important that the blinded key (or at least the keyblob)\n remains live as long as `ctx` is. The IV is safe to free.\n\n @param key Pointer to the blinded key struct.\n @param iv Initialization vector for the encryption function.\n @param[out] ctx Context object for the operation.\n @return Result of the initialization operation."]
    pub fn otcrypto_aes_gcm_encrypt_init(
        key: *mut otcrypto_blinded_key_t,
        iv: otcrypto_const_word32_buf_t,
        ctx: *mut otcrypto_aes_gcm_context_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Initializes the AES-GCM authenticated decryption operation.\n\n The order of operations for decryption is:\n   - `otcrypto_aes_gcm_decrypt_init()` called once\n   - `otcrypto_aes_gcm_update_aad()` called zero or more times\n   - `otcrypto_aes_gcm_update_encrypted_data()` called zero or more times\n   - `otcrypto_aes_gcm_decrypt_final()` called once\n\n Associated data must be added first, before encrypted data; the caller may\n not call `otcrypto_aes_gcm_udpate_aad()` after the first call to\n `otcrypto_aes_gcm_update_encrypted_data()`.\n\n The resulting AES-GCM context will include pointers into the keyblob of the\n blinded key. It is important that the blinded key (or at least the keyblob)\n remains live as long as `ctx` is. The IV is safe to free.\n\n IMPORTANT: Although this routine produces decrypted data incrementally, it\n is the caller's responsibility to ensure that they do not trust the\n decrypted data until the tag check passes.\n\n @param key Pointer to the blinded key struct.\n @param iv Initialization vector for the decryption function.\n @param[out] ctx Context object for the operation.\n @return Result of the initialization operation."]
    pub fn otcrypto_aes_gcm_decrypt_init(
        key: *mut otcrypto_blinded_key_t,
        iv: otcrypto_const_word32_buf_t,
        ctx: *mut otcrypto_aes_gcm_context_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Updates additional authenticated data for an AES-GCM operation.\n\n May be used for either encryption or decryption. Call\n `otcrypto_aes_gcm_encrypt_init` or `otcrypto_aes_gcm_decrypt_init` first.\n\n @param ctx Context object for the operation, updated in place.\n @param aad Additional authenticated data.\n @return Result of the update operation."]
    pub fn otcrypto_aes_gcm_update_aad(
        ctx: *mut otcrypto_aes_gcm_context_t,
        aad: otcrypto_const_byte_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Updates authenticated-and-encrypted data for an AES-GCM operation.\n\n May be used for either encryption or decryption. Call\n `otcrypto_aes_gcm_encrypt_init` or `otcrypto_aes_gcm_decrypt_init` first.\n\n The caller should allocate space for the output and set the `len` field\n accordingly.\n\n For encryption, `input` is the plaintext and `output` is the ciphertext; for\n decryption, they are reversed. The output must always be long enough to\n store all full 128-bit blocks of encrypted data received so far minus all\n output produced so far; rounding the input length to the next 128-bit\n boundary is always enough, but if the caller knows the exact byte-length of\n input so far they can calculate it exactly. Returns an error if `output` is\n not long enough; if `output` is overly long, only the first\n `output_bytes_written` bytes will be used.\n\n @param ctx Context object for the operation, updated in place.\n @param input Plaintext for encryption, ciphertext for decryption.\n @param[out] output Ciphertext for encryption, plaintext for decryption.\n @param[out] output_bytes_written Number of bytes written to `output`.\n @return Result of the update operation."]
    pub fn otcrypto_aes_gcm_update_encrypted_data(
        ctx: *mut otcrypto_aes_gcm_context_t,
        input: otcrypto_const_byte_buf_t,
        output: otcrypto_byte_buf_t,
        output_bytes_written: *mut usize,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finishes the AES-GCM authenticated encryption operation.\n\n Processes any remaining plaintext from the context and computes the\n authentication tag and up to 1 block of ciphertext.\n\n The caller should allocate space for the ciphertext and tag buffers and set\n the `len` fields accordingly. This function returns the\n `ciphertext_bytes_written` parameter with the number of bytes written to\n `ciphertext`, which is always either 16 or 0. This function returns an error\n if the ciphertext or tag buffer is not long enough.\n\n @param ctx Context object for the operation.\n @param tag_len Length of authentication tag to be generated.\n @param[out] ciphertext Encrypted output data.\n @param[out] ciphertext_bytes_written Number of bytes written to `ciphertext`.\n @param[out] auth_tag Generated authentication tag.\n @return Result of the final operation."]
    pub fn otcrypto_aes_gcm_encrypt_final(
        ctx: *mut otcrypto_aes_gcm_context_t,
        tag_len: otcrypto_aes_gcm_tag_len_t,
        ciphertext: otcrypto_byte_buf_t,
        ciphertext_bytes_written: *mut usize,
        auth_tag: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finishes the AES-GCM authenticated decryption operation.\n\n Processes any remaining ciphertext from the context and computes the\n authentication tag and up to 1 block of plaintext.\n\n The caller should allocate space for the plaintext buffer and set the `len`\n field accordingly. This function returns the `ciphertext_bytes_written`\n parameter with the number of bytes written to `ciphertext`. This function\n returns an error if the plaintext buffer is not long enough.\n\n IMPORTANT: the caller must check both the returned status and the `success`\n parameter to know if the tag is valid. The returned status may be OK even if\n the tag check did not succeed, if there were no errors during processing.\n\n @param ctx Context object for the operation.\n @param auth_tag Authentication tag to check.\n @param tag_len Length of authentication tag.\n @param[out] plaintext Decrypted output data.\n @param[out] plaintext_bytes_written Number of bytes written to `plaintext`.\n @param[out] success Whether the tag passed verification.\n @return Result of the final operation."]
    pub fn otcrypto_aes_gcm_decrypt_final(
        ctx: *mut otcrypto_aes_gcm_context_t,
        auth_tag: otcrypto_const_word32_buf_t,
        tag_len: otcrypto_aes_gcm_tag_len_t,
        plaintext: otcrypto_byte_buf_t,
        plaintext_bytes_written: *mut usize,
        success: *mut hardened_bool_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Read the cryptolib build information.\n\n Returns the current version of the cryptolib as well as the\n latest git commit hash of the `sw/device/lib/crypto` directory.\n\n @param ctx Pointer to the generic HMAC context struct.\n @param[out] version The current version of the cryptolib.\n @param[out] build_hash_low The low portion of the git commit hash of\n `sw/device/lib/crypto`.\n @param[out] build_hash_high The high portion of the git commit hash of\n `sw/device/lib/crypto`.\n @return Result of the HMAC final operation."]
    pub fn otcrypto_build_info(
        version: *mut u32,
        released: *mut u8,
        build_hash_low: *mut u32,
        build_hash_high: *mut u32,
    ) -> otcrypto_status_t;
}
#[doc = " The DICE diversifier contains two diversification constants.\n\n - The diversifier is the keymgr 8-word + version diversification constant.\n - The attestation_seed is additional per-chip fixed entropy that is normally\n   stored in the AttestationKeySeeds flash INFO page (bank=0, page=4).  These\n   constants are 320 bits (10 words) long.  Because OTBN's bignum registers\n   are 256 bits wide, we program a full 512 bits to OTBN.  When you load the\n   attestation seed, load 10 words from flash and set the remaining words to\n   zero."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct dice_keymgr_diversifier {
    pub salt: [u32; 8usize],
    pub version: u32,
}
#[doc = " The DICE diversifier contains two diversification constants.\n\n - The diversifier is the keymgr 8-word + version diversification constant.\n - The attestation_seed is additional per-chip fixed entropy that is normally\n   stored in the AttestationKeySeeds flash INFO page (bank=0, page=4).  These\n   constants are 320 bits (10 words) long.  Because OTBN's bignum registers\n   are 256 bits wide, we program a full 512 bits to OTBN.  When you load the\n   attestation seed, load 10 words from flash and set the remaining words to\n   zero."]
pub type dice_keymgr_diversifier_t = dice_keymgr_diversifier;
#[repr(C)]
#[derive(Copy, Clone)]
pub struct dice_diversifier {
    pub diversifier: dice_keymgr_diversifier_t,
    pub attestation_seed: [u32; 16usize],
}
pub type dice_diversifier_t = dice_diversifier;
unsafe extern "C" {
    #[doc = " Generate an ECDSA P256 key from the DICE attestation keymgr.\n\n @param private_key A blinded key with a keyblob of `dice_diversifier_t`.\n @param public_key[out] An unblinded key with a `key` pointer to a 64-byte\n        buffer to receive the P256 x/y coordinates.\n @return OTCRYPTO_OK."]
    pub fn dice_p256_keygen(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Sign a message with an ECDSA P256 key from the DICE attestation keymgr.\n\n @param private_key A blinded key with a keyblob of `dice_diversifier_t`.\n @param message_digest A SHA256 hash of the message to sign.\n @param signature[out] The resulting signature.\n @return OTCRYPTO_OK."]
    pub fn dice_p256_sign(
        private_key: *const otcrypto_blinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        signature: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Verify a message with an ECDSA P256 key from the DICE attestation keymgr.\n\n Note: this is here as a debugging aide.  You should really use\n `otcrypto_p256_verify` to verify signatures.  If you use this function,\n you must check recovered_r to know if the signature was valid.\n\n @param private_key A blinded key with a keyblob of `dice_diversifier_t`.\n @param message_digest The SHA256 hash of the message.\n @param signature The signature to verify.\n @param recovered_r The recovered R portion of the signature.\n @return OTCRYPTO_OK."]
    pub fn dice_p256_verify(
        public_key: *const otcrypto_unblinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        signature: otcrypto_word32_buf_t,
        recovered_r: *mut u32,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Instantiates the DRBG system.\n\n Initializes the DRBG and the context for DRBG. Gets the required entropy\n input automatically from the entropy source.\n\n The personalization string may empty, and may be up to 48 bytes long; any\n longer will result in an error. If the string is word aligned and the size\n is a multiple of the word length (32-bit), it is handled using SCA hardened\n memory operations. If not, a non SCA hardened fallback is used.\n\n @param perso_string Pointer to personalization bitstring.\n @return Result of the DRBG instantiate operation."]
    pub fn otcrypto_drbg_instantiate(perso_string: otcrypto_const_byte_buf_t) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Reseeds the DRBG with fresh entropy.\n\n Reseeds the DRBG with fresh entropy that is automatically fetched from the\n entropy source and updates the working state parameters.\n\n @param additional_input Pointer to the additional input for DRBG.\n @return Result of the DRBG reseed operation."]
    pub fn otcrypto_drbg_reseed(additional_input: otcrypto_const_byte_buf_t) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Instantiates the DRBG system.\n\n Initializes DRBG and the DRBG context. Gets the required entropy input from\n the user through the `entropy` parameter. Calling this function breaks FIPS\n compliance until the DRBG is uninstantiated.\n\n The entropy input must be exactly 384 bits long (48 bytes). The\n personalization string must not be longer than the entropy input, and may be\n empty. If the string is word aligned and the size is a multiple of the word\n length (32-bit), it is handled using SCA hardened memory operations. If not,\n a non SCA hardened fallback is used.\n\n @param entropy Pointer to the user defined entropy value.\n @param personalization_string Pointer to personalization bitstring.\n @return Result of the DRBG manual instantiation."]
    pub fn otcrypto_drbg_manual_instantiate(
        entropy: otcrypto_const_byte_buf_t,
        perso_string: otcrypto_const_byte_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Reseeds the DRBG with fresh entropy.\n\n Reseeds the DRBG with entropy input from the user through the `entropy`\n parameter and updates the working state parameters. Calling this function\n breaks FIPS compliance until the DRBG is uninstantiated.\n\n @param entropy Pointer to the user defined entropy value.\n @param additional_input Pointer to the additional input for DRBG.\n @return Result of the manual DRBG reseed operation."]
    pub fn otcrypto_drbg_manual_reseed(
        entropy: otcrypto_const_byte_buf_t,
        additional_input: otcrypto_const_byte_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " DRBG function for generating random bits.\n\n This function checks the hardware flags for FIPS compatibility of the\n generated bits, so it will fail after `otcrypto_drbg_manual_instantiate` or\n `otcrypto_drbg_manual_reseed`.\n\n The caller should allocate space for the `drbg_output` buffer and set the\n length of expected output in the `len` field.\n\n The output is generated in 16-byte blocks; if `drbg_output->len` is not a\n multiple of 4, some output from the hardware will be discarded. This detail\n may be important for known-answer tests.\n\n @param additional_input Pointer to the additional data.\n @param[out] drbg_output Pointer to the generated pseudo random bits.\n @return Result of the DRBG generate operation."]
    pub fn otcrypto_drbg_generate(
        additional_input: otcrypto_const_byte_buf_t,
        drbg_output: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " DRBG function for generating random bits.\n\n This function does NOT check the hardware flags for FIPS compatibility of the\n generated bits, so it may be called after `otcrypto_drbg_manual_instantiate`\n or `otcrypto_drbg_manual_reseed`.\n\n The caller should allocate space for the `drbg_output` buffer and set the\n length of expected output in the `len` field.\n\n The output is generated in 16-byte blocks; if `drbg_output->len` is not a\n multiple of 4, some output from the hardware will be discarded. This detail\n may be important for known-answer tests.\n\n @param additional_input Pointer to the additional data.\n @param[out] drbg_output Pointer to the generated pseudo random bits.\n @return Result of the DRBG generate operation."]
    pub fn otcrypto_drbg_manual_generate(
        additional_input: otcrypto_const_byte_buf_t,
        drbg_output: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Uninstantiates DRBG and clears the context.\n\n @return Result of the DRBG uninstantiate operation."]
    pub fn otcrypto_drbg_uninstantiate() -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Generates a key pair for ECDSA with curve P-256.\n\n The caller should allocate and partially populate the blinded key struct,\n including populating the key configuration and allocating space for the\n keyblob. For a hardware-backed key, use the private key handle returned by\n `otcrypto_hw_backed_key`. Otherwise, the mode should indicate ECDSA with\n P-256 and the keyblob should be 80 bytes. The value in the `checksum` field\n of the blinded key struct will be populated by the key generation function.\n\n @param[out] private_key Pointer to the blinded private key (d) struct.\n @param[out] public_key Pointer to the unblinded public key (Q) struct.\n @return Result of the ECDSA key generation."]
    pub fn otcrypto_ecdsa_p256_keygen(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Generates an ECDSA signature with curve P-256.\n\n The message digest must be exactly 256 bits (32 bytes) long, but may use any\n hash mode. The caller is responsible for ensuring that the security\n strength of the hash function is at least equal to the security strength of\n the curve, but in some cases it may be truncated. See FIPS 186-5 for\n details.\n\n @param private_key Pointer to the blinded private key (d) struct.\n @param message_digest Message digest to be signed (pre-hashed).\n @param[out] signature Pointer to the signature struct with (r,s) values.\n @return Result of the ECDSA signature generation."]
    pub fn otcrypto_ecdsa_p256_sign(
        private_key: *const otcrypto_blinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        signature: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Generates an ECDSA signature with curve P-256 and verifies the signature\n before releasing it to mitigate fault injection attacks.\n\n The message digest must be exactly 256 bits (32 bytes) long, but may use any\n hash mode. The caller is responsible for ensuring that the security\n strength of the hash function is at least equal to the security strength of\n the curve, but in some cases it may be truncated. See FIPS 186-5 for\n details.\n\n @param private_key Pointer to the blinded private key (d) struct.\n @param public_key Pointer to the unblinded public key (Q) struct.\n @param message_digest Message digest to be signed (pre-hashed).\n @param[out] signature Pointer to the signature struct with (r,s) values.\n @return Result of the ECDSA signature generation."]
    pub fn otcrypto_ecdsa_p256_sign_verify(
        private_key: *const otcrypto_blinded_key_t,
        public_key: *const otcrypto_unblinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        signature: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Verifies an ECDSA/P-256 signature.\n\n The message digest must be exactly 256 bits (32 bytes) long, but may use any\n hash mode. The caller is responsible for ensuring that the security\n strength of the hash function is at least equal to the security strength of\n the curve, but in some cases it may be truncated. See FIPS 186-5 for\n details.\n\n The caller must check the `verification_result` parameter, NOT only the\n returned status code, to know if the signature passed verification. The\n status code, as for other operations, only indicates whether errors were\n encountered, and may return OK even when the signature is invalid.\n\n @param public_key Pointer to the unblinded public key (Q) struct.\n @param message_digest Message digest to be verified (pre-hashed).\n @param signature Pointer to the signature to be verified.\n @param[out] verification_result Whether the signature passed verification.\n @return Result of the ECDSA verification operation."]
    pub fn otcrypto_ecdsa_p256_verify(
        public_key: *const otcrypto_unblinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        signature: otcrypto_const_word32_buf_t,
        verification_result: *mut hardened_bool_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Generates a key pair for ECDH with curve P-256.\n\n The caller should allocate and partially populate the blinded key struct,\n including populating the key configuration and allocating space for the\n keyblob. For a hardware-backed key, use the private key handle returned by\n `otcrypto_hw_backed_key`. Otherwise, the mode should indicate ECDH with\n P-256 and the keyblob should be 80 bytes. The value in the `checksum` field\n of the blinded key struct will be populated by the key generation function.\n\n @param[out] private_key Pointer to the blinded private key (d) struct.\n @param[out] public_key Pointer to the unblinded public key (Q) struct.\n @return Result of the ECDH key generation."]
    pub fn otcrypto_ecdh_p256_keygen(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Elliptic Curve Diffie Hellman shared secret generation with curve P-256.\n\n @param private_key Pointer to the blinded private key (d) struct.\n @param public_key Pointer to the unblinded public key (Q) struct.\n @param[out] shared_secret Pointer to generated blinded shared key struct.\n @return Result of ECDH shared secret generation."]
    pub fn otcrypto_ecdh_p256(
        private_key: *const otcrypto_blinded_key_t,
        public_key: *const otcrypto_unblinded_key_t,
        shared_secret: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous key generation for ECDSA/P-256.\n\n See `otcrypto_ecdsa_p256_keygen` for requirements on input values.\n\n @param private_key Destination structure for private key, or key handle.\n @return Result of asynchronous ECDSA keygen start operation."]
    pub fn otcrypto_ecdsa_p256_keygen_async_start(
        private_key: *const otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous key generation for ECDSA/P-256.\n\n See `otcrypto_ecdsa_p256_keygen` for requirements on input values.\n\n May block until the operation is complete.\n\n The caller should ensure that the private key configuration matches that\n passed to the `_start` function.\n\n @param[out] private_key Pointer to the blinded private key (d) struct.\n @param[out] public_key Pointer to the unblinded public key (Q) struct.\n @return Result of asynchronous ECDSA keygen finalize operation."]
    pub fn otcrypto_ecdsa_p256_keygen_async_finalize(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous signature generation for ECDSA/P-256.\n\n See `otcrypto_ecdsa_p256_sign` for requirements on input values.\n\n @param private_key Pointer to the blinded private key (d) struct.\n @param message_digest Message digest to be signed (pre-hashed).\n @return Result of async ECDSA start operation."]
    pub fn otcrypto_ecdsa_p256_sign_async_start(
        private_key: *const otcrypto_blinded_key_t,
        message_digest: otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous signature generation for ECDSA/P-256.\n\n See `otcrypto_ecdsa_p256_sign` for requirements on input values.\n\n May block until the operation is complete.\n\n @param[out] signature Pointer to the signature struct with (r,s) values.\n @return Result of async ECDSA finalize operation."]
    pub fn otcrypto_ecdsa_p256_sign_async_finalize(
        signature: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous signature verification for ECDSA/P-256.\n\n See `otcrypto_ecdsa_p256_verify` for requirements on input values.\n\n @param public_key Pointer to the unblinded public key (Q) struct.\n @param message_digest Message digest to be verified (pre-hashed).\n @param signature Pointer to the signature to be verified.\n @return Result of async ECDSA verify start function."]
    pub fn otcrypto_ecdsa_p256_verify_async_start(
        public_key: *const otcrypto_unblinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        signature: otcrypto_const_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous signature verification for ECDSA/P-256.\n\n See `otcrypto_ecdsa_p256_verify` for requirements on input values.\n\n May block until the operation is complete.\n\n The caller must check the `verification_result` parameter, NOT only the\n returned status code, to know if the signature passed verification. The\n status code, as for other operations, only indicates whether errors were\n encountered, and may return OK even when the signature is invalid.\n\n @param[out] verification_result Whether the signature passed verification.\n @return Result of async ECDSA verify finalize operation."]
    pub fn otcrypto_ecdsa_p256_verify_async_finalize(
        signature: otcrypto_const_word32_buf_t,
        verification_result: *mut hardened_bool_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous key generation for ECDH/P-256.\n\n See `otcrypto_ecdh_p256_keygen` for requirements on input values.\n\n @param private_key Destination structure for private key, or key handle.\n @return Result of asynchronous ECDH keygen start operation."]
    pub fn otcrypto_ecdh_p256_keygen_async_start(
        private_key: *const otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous key generation for ECDH/P-256.\n\n See `otcrypto_ecdh_p256_keygen` for requirements on input values.\n\n May block until the operation is complete.\n\n The caller should ensure that the private key configuration matches that\n passed to the `_start` function.\n\n @param[out] private_key Pointer to the blinded private key (d) struct.\n @param[out] public_key Pointer to the unblinded public key (Q) struct.\n @return Result of asynchronous ECDH keygen finalize operation."]
    pub fn otcrypto_ecdh_p256_keygen_async_finalize(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous shared secret generation for ECDH/P-256.\n\n See `otcrypto_ecdh_p256` for requirements on input values.\n\n @param private_key Pointer to the blinded private key (d) struct.\n @param public_key Pointer to the unblinded public key (Q) struct.\n @return Result of async ECDH start operation."]
    pub fn otcrypto_ecdh_p256_async_start(
        private_key: *const otcrypto_blinded_key_t,
        public_key: *const otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous shared secret generation for ECDH/P-256.\n\n See `otcrypto_ecdh_p256` for requirements on input values.\n\n May block until the operation is complete.\n\n @param[out] shared_secret Pointer to generated blinded shared key struct.\n @return Result of async ECDH finalize operation."]
    pub fn otcrypto_ecdh_p256_async_finalize(
        shared_secret: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Generates a key pair for ECDSA with curve P-384.\n\n The caller should allocate and partially populate the blinded key struct,\n including populating the key configuration and allocating space for the\n keyblob. For a hardware-backed key, use the private key handle returned by\n `otcrypto_hw_backed_key`. Otherwise, the mode should indicate ECDSA with\n P-384 and the keyblob should be 112 bytes. The value in the `checksum` field\n of the blinded key struct will be populated by the key generation function.\n\n @param[out] private_key Pointer to the blinded private key (d) struct.\n @param[out] public_key Pointer to the unblinded public key (Q) struct.\n @return Result of the ECDSA key generation."]
    pub fn otcrypto_ecdsa_p384_keygen(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Generates an ECDSA signature with curve P-384.\n\n The message digest must be exactly 384 bits (48 bytes) long, but may use any\n hash mode. The caller is responsible for ensuring that the security\n strength of the hash function is at least equal to the security strength of\n the curve, but in some cases it may be truncated. See FIPS 186-5 for\n details.\n\n @param private_key Pointer to the blinded private key (d) struct.\n @param message_digest Message digest to be signed (pre-hashed).\n @param[out] signature Pointer to the signature struct with (r,s) values.\n @return Result of the ECDSA signature generation."]
    pub fn otcrypto_ecdsa_p384_sign(
        private_key: *const otcrypto_blinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        signature: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Generates an ECDSA signature with curve P-384 and verifies the signature\n before releasing it to mitigate fault injection attacks.\n\n The message digest must be exactly 384 bits (48 bytes) long, but may use any\n hash mode. The caller is responsible for ensuring that the security\n strength of the hash function is at least equal to the security strength of\n the curve, but in some cases it may be truncated. See FIPS 186-5 for\n details.\n\n @param private_key Pointer to the blinded private key (d) struct.\n @param public_key Pointer to the unblinded public key (Q) struct.\n @param message_digest Message digest to be signed (pre-hashed).\n @param[out] signature Pointer to the signature struct with (r,s) values.\n @return Result of the ECDSA signature generation."]
    pub fn otcrypto_ecdsa_p384_sign_verify(
        private_key: *const otcrypto_blinded_key_t,
        public_key: *const otcrypto_unblinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        signature: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Verifies an ECDSA/P-384 signature.\n\n The message digest must be exactly 384 bits (48 bytes) long, but may use any\n hash mode. The caller is responsible for ensuring that the security\n strength of the hash function is at least equal to the security strength of\n the curve, but in some cases it may be truncated. See FIPS 186-5 for\n details.\n\n The caller must check the `verification_result` parameter, NOT only the\n returned status code, to know if the signature passed verification. The\n status code, as for other operations, only indicates whether errors were\n encountered, and may return OK even when the signature is invalid.\n\n @param public_key Pointer to the unblinded public key (Q) struct.\n @param message_digest Message digest to be verified (pre-hashed).\n @param signature Pointer to the signature to be verified.\n @param[out] verification_result Whether the signature passed verification.\n @return Result of the ECDSA verification operation."]
    pub fn otcrypto_ecdsa_p384_verify(
        public_key: *const otcrypto_unblinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        signature: otcrypto_const_word32_buf_t,
        verification_result: *mut hardened_bool_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Generates a key pair for ECDH with curve P-384.\n\n The caller should allocate and partially populate the blinded key struct,\n including populating the key configuration and allocating space for the\n keyblob. For a hardware-backed key, use the private key handle returned by\n `otcrypto_hw_backed_key`. Otherwise, the mode should indicate ECDH with\n P-384 and the keyblob should be 112 bytes. The value in the `checksum` field\n of the blinded key struct will be populated by the key generation function.\n\n @param[out] private_key Pointer to the blinded private key (d) struct.\n @param[out] public_key Pointer to the unblinded public key (Q) struct.\n @return Result of the ECDH key generation."]
    pub fn otcrypto_ecdh_p384_keygen(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Elliptic Curve Diffie Hellman shared secret generation with curve P-384.\n\n @param private_key Pointer to the blinded private key (d) struct.\n @param public_key Pointer to the unblinded public key (Q) struct.\n @param[out] shared_secret Pointer to generated blinded shared key struct.\n @return Result of ECDH shared secret generation."]
    pub fn otcrypto_ecdh_p384(
        private_key: *const otcrypto_blinded_key_t,
        public_key: *const otcrypto_unblinded_key_t,
        shared_secret: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous key generation for ECDSA/P-384.\n\n See `otcrypto_ecdsa_p384_keygen` for requirements on input values.\n\n @param private_key Destination structure for private key, or key handle.\n @return Result of asynchronous ECDSA keygen start operation."]
    pub fn otcrypto_ecdsa_p384_keygen_async_start(
        private_key: *const otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous key generation for ECDSA/P-384.\n\n See `otcrypto_ecdsa_p384_keygen` for requirements on input values.\n\n May block until the operation is complete.\n\n The caller should ensure that the private key configuration matches that\n passed to the `_start` function.\n\n @param[out] private_key Pointer to the blinded private key (d) struct.\n @param[out] public_key Pointer to the unblinded public key (Q) struct.\n @return Result of asynchronous ECDSA keygen finalize operation."]
    pub fn otcrypto_ecdsa_p384_keygen_async_finalize(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous signature generation for ECDSA/P-384.\n\n See `otcrypto_ecdsa_p384_sign` for requirements on input values.\n\n @param private_key Pointer to the blinded private key (d) struct.\n @param message_digest Message digest to be signed (pre-hashed).\n @return Result of async ECDSA start operation."]
    pub fn otcrypto_ecdsa_p384_sign_async_start(
        private_key: *const otcrypto_blinded_key_t,
        message_digest: otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous signature generation for ECDSA/P-384.\n\n See `otcrypto_ecdsa_p384_sign` for requirements on input values.\n\n May block until the operation is complete.\n\n @param[out] signature Pointer to the signature struct with (r,s) values.\n @return Result of async ECDSA finalize operation."]
    pub fn otcrypto_ecdsa_p384_sign_async_finalize(
        signature: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous signature verification for ECDSA/P-384.\n\n See `otcrypto_ecdsa_p384_verify` for requirements on input values.\n\n @param public_key Pointer to the unblinded public key (Q) struct.\n @param message_digest Message digest to be verified (pre-hashed).\n @param signature Pointer to the signature to be verified.\n @return Result of async ECDSA verify start function."]
    pub fn otcrypto_ecdsa_p384_verify_async_start(
        public_key: *const otcrypto_unblinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        signature: otcrypto_const_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous signature verification for ECDSA/P-384.\n\n See `otcrypto_ecdsa_p384_verify` for requirements on input values.\n\n May block until the operation is complete.\n\n The caller must check the `verification_result` parameter, NOT only the\n returned status code, to know if the signature passed verification. The\n status code, as for other operations, only indicates whether errors were\n encountered, and may return OK even when the signature is invalid.\n\n @param[out] verification_result Whether the signature passed verification.\n @return Result of async ECDSA verify finalize operation."]
    pub fn otcrypto_ecdsa_p384_verify_async_finalize(
        signature: otcrypto_const_word32_buf_t,
        verification_result: *mut hardened_bool_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous key generation for ECDH/P-384.\n\n See `otcrypto_ecdh_p384_keygen` for requirements on input values.\n\n @param private_key Destination structure for private key, or key handle.\n @return Result of asynchronous ECDH keygen start operation."]
    pub fn otcrypto_ecdh_p384_keygen_async_start(
        private_key: *const otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous key generation for ECDH/P-384.\n\n See `otcrypto_ecdh_p384_keygen` for requirements on input values.\n\n May block until the operation is complete.\n\n The caller should ensure that the private key configuration matches that\n passed to the `_start` function.\n\n @param[out] private_key Pointer to the blinded private key (d) struct.\n @param[out] public_key Pointer to the unblinded public key (Q) struct.\n @return Result of asynchronous ECDH keygen finalize operation."]
    pub fn otcrypto_ecdh_p384_keygen_async_finalize(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous shared secret generation for ECDH/P-384.\n\n See `otcrypto_ecdh_p384` for requirements on input values.\n\n @param private_key Pointer to the blinded private key (d) struct.\n @param public_key Pointer to the unblinded public key (Q) struct.\n @return Result of async ECDH start operation."]
    pub fn otcrypto_ecdh_p384_async_start(
        private_key: *const otcrypto_blinded_key_t,
        public_key: *const otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous shared secret generation for ECDH/P-384.\n\n See `otcrypto_ecdh_p384` for requirements on input values.\n\n May block until the operation is complete.\n\n @param[out] shared_secret Pointer to generated blinded shared key struct.\n @return Result of async ECDH finalize operation."]
    pub fn otcrypto_ecdh_p384_async_finalize(
        shared_secret: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
pub const otcrypto_eddsa_sign_mode_kOtcryptoEddsaSignModeEddsa: otcrypto_eddsa_sign_mode = 2785;
pub const otcrypto_eddsa_sign_mode_kOtcryptoEddsaSignModeHashEddsa: otcrypto_eddsa_sign_mode = 2470;
#[doc = " Hashing mode for EdDSA signatures.\n\n Values are hardened."]
pub type otcrypto_eddsa_sign_mode = ::core::ffi::c_uint;
#[doc = " Hashing mode for EdDSA signatures.\n\n Values are hardened."]
pub use self::otcrypto_eddsa_sign_mode as otcrypto_eddsa_sign_mode_t;
unsafe extern "C" {
    #[doc = " Generates a key pair for Ed25519.\n\n The caller should allocate and partially populate the blinded key struct,\n including populating the key configuration and allocating space for the\n keyblob. For a hardware-backed key, use the private key handle returned by\n `otcrypto_hw_backed_key`. Otherwise, the mode should indicate Ed25519 and the\n keyblob should be 80 bytes. The value in the `checksum` field of the blinded\n key struct will be populated by the key generation function.\n\n @param[out] private_key Pointer to the blinded private key struct.\n @param[out] public_key Pointer to the unblinded public key struct.\n @return Result of the Ed25519 key generation."]
    pub fn otcrypto_ed25519_keygen(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Generates an Ed25519 digital signature.\n\n @param private_key Pointer to the blinded private key struct.\n @param input_message Input message to be signed.\n @param sign_mode EdDSA signature hashing mode.\n @param[out] signature Pointer to the EdDSA signature with (r,s) values.\n @return Result of the Ed25519 signature generation."]
    pub fn otcrypto_ed25519_sign(
        private_key: *const otcrypto_blinded_key_t,
        input_message: otcrypto_const_byte_buf_t,
        sign_mode: otcrypto_eddsa_sign_mode_t,
        signature: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Verifies an Ed25519 signature.\n\n The caller must check the `verification_result` parameter, NOT only the\n returned status code, to know if the signature passed verification. The\n status code, as for other operations, only indicates whether errors were\n encountered, and may return OK even when the signature is invalid.\n\n @param public_key Pointer to the unblinded public key struct.\n @param input_message Input message to be signed for verification.\n @param sign_mode EdDSA signature hashing mode.\n @param signature Pointer to the signature to be verified.\n @param[out] verification_result Whether the signature passed verification.\n @return Result of the Ed25519 verification operation."]
    pub fn otcrypto_ed25519_verify(
        public_key: *const otcrypto_unblinded_key_t,
        input_message: otcrypto_const_byte_buf_t,
        sign_mode: otcrypto_eddsa_sign_mode_t,
        signature: otcrypto_const_word32_buf_t,
        verification_result: *mut hardened_bool_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous key generation for Ed25519.\n\n See `otcrypto_ed25519_keygen` for requirements on input values.\n\n @param private_key Destination structure for private key, or key handle.\n @return Result of asynchronous Ed25519 keygen start operation."]
    pub fn otcrypto_ed25519_keygen_async_start(
        private_key: *const otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous key generation for Ed25519.\n\n See `otcrypto_ed25519_keygen` for requirements on input values.\n\n May block until the operation is complete.\n\n The caller should ensure that the private key configuration matches that\n passed to the `_start` function.\n\n @param[out] private_key Pointer to the blinded private key struct.\n @param[out] public_key Pointer to the unblinded public key struct.\n @return Result of asynchronous ed25519 keygen finalize operation."]
    pub fn otcrypto_ed25519_keygen_async_finalize(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous signature generation for Ed25519.\n\n See `otcrypto_ed25519_sign` for requirements on input values.\n\n @param private_key Pointer to the blinded private key struct.\n @param input_message Input message to be signed.\n @param sign_mode EdDSA signature hashing mode.\n @param[out] signature Pointer to the EdDSA signature to get (r) value.\n @return Result of async Ed25519 start operation."]
    pub fn otcrypto_ed25519_sign_async_start(
        private_key: *const otcrypto_blinded_key_t,
        input_message: otcrypto_const_byte_buf_t,
        sign_mode: otcrypto_eddsa_sign_mode_t,
        signature: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous signature generation for Ed25519.\n\n See `otcrypto_ecdsa_p256_sign` for requirements on input values.\n\n May block until the operation is complete.\n\n @param[out] signature Pointer to the EdDSA signature to get (s) value.\n @return Result of async Ed25519 finalize operation."]
    pub fn otcrypto_ed25519_sign_async_finalize(
        signature: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous signature verification for Ed25519.\n\n See `otcrypto_ecdsa_p256_verify` for requirements on input values.\n\n @param public_key Pointer to the unblinded public key struct.\n @param input_message Input message to be signed for verification.\n @param sign_mode EdDSA signature hashing mode.\n @param signature Pointer to the signature to be verified.\n @return Result of async Ed25519 verification start operation."]
    pub fn otcrypto_ed25519_verify_async_start(
        public_key: *const otcrypto_unblinded_key_t,
        input_message: otcrypto_const_byte_buf_t,
        sign_mode: otcrypto_eddsa_sign_mode_t,
        signature: otcrypto_const_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous signature verification for Ed25519.\n\n See `otcrypto_ecdsa_p256_verify` for requirements on input values.\n\n May block until the operation is complete.\n\n The caller must check the `verification_result` parameter, NOT only the\n returned status code, to know if the signature passed verification. The\n status code, as for other operations, only indicates whether errors were\n encountered, and may return OK even when the signature is invalid.\n\n @param[out] verification_result Whether the signature passed verification.\n @return Result of async Ed25519 verification finalize operation."]
    pub fn otcrypto_ed25519_verify_async_finalize(
        verification_result: *mut hardened_bool_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Performs HKDF (IETF RFC 5869) in one shot, both expand and extract stages.\n\n The hash mode for the underlying HMAC is determined by the mode of the input\n key material, e.g. the key mode `kOtcryptoKeyModeHmacSha256` results in HMAC\n with SHA-256.\n\n The caller should allocate and partially populate the `okm` blinded key\n struct, including populating the key configuration and allocating space for\n the keyblob. The configuration may not indicate a hardware-backed key and\n must indicate a symmetric mode. The allocated keyblob length for the output\n key should be twice the unmasked key length indicated in its key\n configuration, rounded up to the nearest 32-bit word. This unmasked key\n length must not be longer than 255*<length of digest for the chosen hash\n mode>, as per the RFC. The value in the `checksum` field of the blinded key\n struct will be populated by the key derivation function.\n\n @param ikm Blinded input key material.\n @param salt Salt value (optional, may be empty).\n @param info Context-specific string (optional, may be empty).\n @param[out] okm Blinded output keying material.\n @return Result of the key derivation operation."]
    pub fn otcrypto_hkdf(
        ikm: *const otcrypto_blinded_key_t,
        salt: otcrypto_const_byte_buf_t,
        info: otcrypto_const_byte_buf_t,
        okm: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Performs the \"extract\" step of HKDF (IETF RFC 5869).\n\n The hash mode for the underlying HMAC is determined by the mode of the input\n key material, e.g. the key mode `kOtcryptoKeyModeHmacSha256` results in HMAC\n with SHA-256.\n\n The caller should allocate and partially populate the `prk` blinded key\n struct, including populating the key configuration and allocating space for\n the keyblob. The configuration may not indicate a hardware-backed key, and\n the mode should be the same HMAC mode as the input keying material. The\n allocated keyblob length should be twice the hash function digest length for\n the chosen hash mode (e.g. 512 bits for SHA-256).\n\n @param ikm Blinded input key material.\n @param salt Salt value (optional, may be empty).\n @param[out] prk Extracted pseudo-random key.\n @return Result of the key derivation operation."]
    pub fn otcrypto_hkdf_extract(
        ikm: *const otcrypto_blinded_key_t,
        salt: otcrypto_const_byte_buf_t,
        prk: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Performs the \"expand\" step of HKDF (IETF RFC 5869).\n\n The hash mode for the underlying HMAC is determined by the mode of the\n pseudo-random key, e.g. the key mode `kOtcryptoKeyModeHmacSha256` results in\n HMAC with SHA-256.\n\n See `otcrypto_hkdf` for the requirements on `okm`.\n\n @param prk Pseudo-random key from HKDF-extract.\n @param info Context-specific string (optional).\n @param[out] okm Blinded output key material.\n @return Result of the key derivation operation."]
    pub fn otcrypto_hkdf_expand(
        prk: *const otcrypto_blinded_key_t,
        info: otcrypto_const_byte_buf_t,
        okm: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
#[doc = " Opaque SHA-2 hash context.\n\n Representation is internal to the hash implementation; initialize\n with #otcrypto_sha2_init."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct otcrypto_sha2_context {
    pub data: [u32; 88usize],
}
#[doc = " Opaque SHA-2 hash context.\n\n Representation is internal to the hash implementation; initialize\n with #otcrypto_sha2_init."]
pub type otcrypto_sha2_context_t = otcrypto_sha2_context;
unsafe extern "C" {
    #[doc = " One-shot SHA2-256 hash computation.\n\n The caller should allocate space for the `digest` buffer and set the `len`\n fields. If the length does not match the mode, an error message will be\n returned. The `mode` field will be set by this function.\n\n @param message Input message to be hashed.\n @param[out] digest Output digest after hashing the input message.\n @return OK or error."]
    pub fn otcrypto_sha2_256(
        message: otcrypto_const_byte_buf_t,
        digest: *mut otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " One-shot SHA2-384 hash computation.\n\n The caller should allocate space for the `digest` buffer and set the `len`\n fields. If the length does not match the mode, an error message will be\n returned. The `mode` field will be set by this function.\n\n @param message Input message to be hashed.\n @param[out] digest Output digest after hashing the input message.\n @return OK or error."]
    pub fn otcrypto_sha2_384(
        message: otcrypto_const_byte_buf_t,
        digest: *mut otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " One-shot SHA2-512 hash computation.\n\n The caller should allocate space for the `digest` buffer and set the `len`\n field. If the length does not match the mode, an error message will be\n returned. The `mode` field will be set by this function.\n\n @param message Input message to be hashed.\n @param[out] digest Output digest after hashing the input message.\n @return OK or error."]
    pub fn otcrypto_sha2_512(
        message: otcrypto_const_byte_buf_t,
        digest: *mut otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Start a streaming SHA2 operation.\n\n @param hash_mode Desired mode (must be a SHA-2 mode).\n @param[out] ctx Initialized context object.\n @return OK or error."]
    pub fn otcrypto_sha2_init(
        hash_mode: otcrypto_hash_mode_t,
        ctx: *mut otcrypto_sha2_context_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Add more data to a streaming SHA2 operation and update the context.\n\n @param ctx Initialized context object (updated in place).\n @param message Input message data.\n @return OK or error."]
    pub fn otcrypto_sha2_update(
        ctx: *mut otcrypto_sha2_context_t,
        message: otcrypto_const_byte_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finish a streaming SHA2 operation.\n\n The caller should allocate space for the `digest` buffer and set the `len`\n field. If the length does not match the context, an error message will be\n returned. The `mode` field will be inferred from the length and set by this\n function.\n\n The context data should not be used after this operation.\n\n @param ctx Initialized context object.\n @param[out] digest Resulting digest.\n @return OK or error."]
    pub fn otcrypto_sha2_final(
        ctx: *mut otcrypto_sha2_context_t,
        digest: *mut otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
#[doc = " Generic hmac context.\n\n Representation is internal to the hmac implementation; initialize\n with #otcrypto_hmac_init."]
#[repr(C)]
#[derive(Copy, Clone)]
pub struct otcrypto_hmac_context {
    pub data: [u32; 88usize],
}
#[doc = " Generic hmac context.\n\n Representation is internal to the hmac implementation; initialize\n with #otcrypto_hmac_init."]
pub type otcrypto_hmac_context_t = otcrypto_hmac_context;
unsafe extern "C" {
    #[doc = " Performs the HMAC function on the input data.\n\n This function computes the HMAC function on the `input_message` using the\n `key` and returns a `tag`. The key should be at least as long as the digest\n for the chosen hash function. The hash function is determined by the key\n mode. Only SHA-2 hash functions are supported. Other modes (e.g. SHA-3) are\n not supported and will result in errors.\n\n The caller should allocate the following amount of space for the `tag`\n buffer, depending on which hash algorithm is used:\n\n SHA-256: 32 bytes\n SHA-384: 48 bytes\n SHA-512: 64 bytes\n\n The caller should also set the `len` field of `tag` to the equivalent number\n of 32-bit words (e.g. 8 for SHA-256).\n\n @param key Pointer to the blinded key struct with key shares.\n @param input_message Input message to be hashed.\n @param[out] tag Output authentication tag.\n @return The result of the HMAC operation."]
    pub fn otcrypto_hmac(
        key: *const otcrypto_blinded_key_t,
        input_message: otcrypto_const_byte_buf_t,
        tag: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Performs the INIT operation for HMAC.\n\n Initializes the HMAC context. The key should be at least as long as the\n digest for the chosen hash function. The hash function is determined by the\n key mode. Only SHA-2 hash functions are are supported. Other modes (e.g.\n SHA-3) are not supported and will result in errors.\n\n @param[out] ctx Pointer to the generic HMAC context struct.\n @param key Pointer to the blinded HMAC key struct.\n @param hash_mode Hash function to use.\n @return Result of the HMAC init operation."]
    pub fn otcrypto_hmac_init(
        ctx: *mut otcrypto_hmac_context_t,
        key: *const otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Performs the UPDATE operation for HMAC.\n\n The update operation processes the `input_message` using the selected\n compression function. The intermediate state is stored in the HMAC context\n `ctx`. Any partial data is stored back in the context and combined with the\n subsequent bytes.\n\n #otcrypto_hmac_init should be called before calling this function.\n\n @param ctx Pointer to the generic HMAC context struct.\n @param input_message Input message to be hashed.\n @return Result of the HMAC update operation."]
    pub fn otcrypto_hmac_update(
        ctx: *mut otcrypto_hmac_context_t,
        input_message: otcrypto_const_byte_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Performs the FINAL operation for HMAC.\n\n The final operation processes the remaining partial blocks, computes the\n final authentication code and copies it to the `tag` parameter.\n\n #otcrypto_hmac_update should be called before calling this function.\n\n The caller should allocate space for the `tag` buffer, (the length should\n match the hash function digest size), and set the length of expected output\n in the `len` field of `tag`. If the user-set length and the output length\n does not match, an error message will be returned.\n\n @param ctx Pointer to the generic HMAC context struct.\n @param[out] tag Output authentication tag.\n @return Result of the HMAC final operation."]
    pub fn otcrypto_hmac_final(
        ctx: *mut otcrypto_hmac_context_t,
        tag: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Performs KDF-CTR with HMAC as the PRF, according to NIST SP 800-108r1.\n\n The caller should allocate and partially populate the `output_key_material`\n blinded key struct, including populating the key configuration and\n allocating space for the keyblob. The configuration may not indicate a\n hardware-backed key and must indicate a symmetric mode. The allocated\n keyblob length for the output key should be twice the unmasked key length\n indicated in its key configuration, rounded up to the nearest 32-bit word.\n The value in the `checksum` field of the blinded key struct will be\n populated by the key derivation function.\n\n @param key_derivation_key Blinded key derivation key.\n @param label Label string (optional, may be empty).\n @param context Context string (optional, may be empty).\n @param[out] output_key_material Blinded output key material.\n @return Result of the key derivation operation."]
    pub fn otcrypto_kdf_ctr_hmac(
        key_derivation_key: *const otcrypto_blinded_key_t,
        label: otcrypto_const_byte_buf_t,
        context: otcrypto_const_byte_buf_t,
        output_key_material: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Generates a new, random symmetric key.\n\n Use this only for symmetric algorithms (e.g. AES, HMAC, KMAC). Asymmetric\n algorithms (e.g. ECDSA, RSA) have their own specialized key-generation\n routines. Cannot be used for hardware-backed keys; use\n `otcrypto_hw_backed_key` instead to generate these.\n\n The caller should allocate space for the keyblob and populate the blinded\n key struct with the length of the keyblob, the pointer to the keyblob\n buffer, and the key configuration. The value in the `checksum` field of\n the blinded key struct will be populated by the key generation function.\n The keyblob should be twice the length of the unblinded key.  This function\n will return an error if the keyblob length does not match expectations based\n on the key mode and configuration.\n\n The keyblob should be twice the length of the key. The caller only needs to\n allocate the keyblob, not populate it.\n\n The personalization string may empty, and may be up to 48 bytes long; any\n longer will result in an error. It is passed as an extra seed input to the\n DRBG, in addition to the hardware TRNG.\n\n @param perso_string Optional personalization string to be passed to DRBG.\n @param[out] key Destination blinded key struct.\n @return The result of the operation."]
    pub fn otcrypto_symmetric_keygen(
        perso_string: otcrypto_const_byte_buf_t,
        key: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Creates a handle for a hardware-backed key.\n\n This routine may be used for both symmetric and asymmetric algorithms, since\n conceptually it only creates some data that the key manager can use to\n generate key material at the time of use. However, not all algorithms are\n suitable for hardware-backed keys (for example, RSA is not suitable), so\n some choices of algorithm may result in errors.\n\n The caller should partially populate the blinded key struct; they should set\n the full key configuration and the keyblob length (always 32 bytes), and\n then allocate 32 bytes of space for the keyblob and set the keyblob pointer.\n\n This function will populate the `checksum` field and copy the salt/version\n data into the keyblob buffer in the specific order that the rest of\n cryptolib expects.\n\n @param version Key version.\n @param salt Key salt (diversification data for KDF).\n @param[out] key Destination blinded key struct.\n @return The result of the operation."]
    pub fn otcrypto_hw_backed_key(
        version: u32,
        salt: *const u32,
        key: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Returns the length that the blinded key will have once wrapped.\n\n @param config Key configuration.\n @param[out] wrapped_num_words Number of 32b words for the wrapped key.\n @return Result of the operation."]
    pub fn otcrypto_wrapped_key_len(
        config: otcrypto_key_config_t,
        wrapped_num_words: *mut usize,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Wraps (encrypts) a secret key.\n\n The key wrap function uses AES-KWP (key wrapping with padding), an\n authenticated encryption mode designed for encrypting key material.\n\n The caller should allocate space for the `wrapped_key` buffer according to\n `otcrypto_wrapped_key_len`, and set the length of expected output in the\n `len` field of `wrapped_key`. If the user-set length and the output length\n do not match, an error message will be returned.\n\n The blinded key struct to wrap must be 32-bit aligned.\n\n @param key_to_wrap Blinded key that will be encrypted.\n @param key_kek AES-KWP key used to encrypt `key_to_wrap`.\n @param[out] wrapped_key Encrypted key data.\n @return Result of the wrap operation."]
    pub fn otcrypto_key_wrap(
        key_to_wrap: *const otcrypto_blinded_key_t,
        key_kek: *const otcrypto_blinded_key_t,
        wrapped_key: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Unwraps (decrypts) a secret key.\n\n The key unwrap function uses AES-KWP (key wrapping with padding), an\n authenticated encryption mode designed for encrypting key material.\n\n The caller must allocate space for the keyblob and set the keyblob-length\n and keyblob fields in `unwrapped_key` accordingly. If there is not enough\n space in the keyblob, this function will return an error. Too much space in\n the keyblob is okay; this function will write to the first part of the\n keyblob buffer and set the keyblob length field to the correct exact value\n for the unwrapped key, at which point it is safe to check the new length and\n free the remaining keyblob memory. It is always safe to allocate a keyblob\n the same size as the wrapped key; this will always be enough space by\n definition.\n\n The caller does not need to populate the blinded key configuration, since\n this information is encrypted along with the key.  However, the caller may\n want to check that the configuration matches expectations.\n\n An OK status from this function does NOT necessarily mean that unwrapping\n succeeded; the caller must check both the returned status and the `success`\n parameter before reading the unwrapped key.\n\n @param wrapped_key Encrypted key data.\n @param key_kek AES-KWP key used to decrypt `wrapped_key`.\n @param[out] success Whether the wrapped key was valid.\n @param[out] unwrapped_key Decrypted key data.\n @return Result of the aes-kwp unwrap operation."]
    pub fn otcrypto_key_unwrap(
        wrapped_key: otcrypto_const_word32_buf_t,
        key_kek: *const otcrypto_blinded_key_t,
        success: *mut hardened_bool_t,
        unwrapped_key: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Creates a blinded key struct from masked key material.\n\n The caller should allocate and partially populate the blinded key struct,\n including populating the key configuration and allocating space for the\n keyblob. The keyblob should be twice the length of the user key.\n Hardware-backed and asymmetric (ECC or RSA) keys cannot be imported this\n way. For asymmetric keys, use algorithm-specific key construction methods.\n\n This function will copy the data from the shares into the keyblob; it is\n safe to free `key_share0` and `key_share1` after this call.\n\n @param key_share0 First share of the user provided key.\n @param key_share1 Second share of the user provided key.\n @param[out] blinded_key Generated blinded key struct.\n @return Result of the blinded key import operation."]
    pub fn otcrypto_import_blinded_key(
        key_share0: otcrypto_const_word32_buf_t,
        key_share1: otcrypto_const_word32_buf_t,
        blinded_key: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Exports a blinded key to the user provided key buffer, in shares.\n\n This function will copy data from the keyblob into the shares; after the\n call, it is safe to free the blinded key and the data pointed to by\n `blinded_key.keyblob`.\n\n Hardware-backed, non-exportable, and asymmetric (ECC or RSA) keys cannot be\n exported this way. For asymmetric keys, use an algorithm-specific funtion.\n\n @param blinded_key Blinded key struct to be exported.\n @param[out] key_share0 First share of the blinded key.\n @param[out] key_share1 Second share of the blinded key.\n @return Result of the blinded key export operation."]
    pub fn otcrypto_export_blinded_key(
        blinded_key: *const otcrypto_blinded_key_t,
        key_share0: otcrypto_word32_buf_t,
        key_share1: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Performs the KMAC function on the input data.\n\n This function computes the KMAC on the `input_message` using the `key` and\n returns a `tag` of `required_output_len`. The customization string is passed\n through `customization_string` parameter. If no customization is desired it\n can be be left empty (by settings its `data` to NULL and `length` to 0).\n\n The caller should set the `key_length` field of `key.config` to the number\n of bytes in the key. Only the following key sizes (in bytes) are supported:\n [16, 24, 32, 48, 64]. If any other size is given, the function will return\n an error.\n\n The KMAC mode (KMAC-128 or KMAC-256) is inferred from the key mode.\n\n The caller should allocate enough space in the `tag` buffer to hold\n `required_output_len` bytes, rounded up to the nearest word, and then set\n the `len` field of `tag` to the word length. If the word length is not long\n enough to hold `required_output_len` bytes, then the function will return an\n error.\n\n @param key Pointer to the blinded key struct with key shares.\n @param input_message Input message to be hashed.\n @param customization_string Customization string.\n @param required_output_len Required output length, in bytes.\n @param[out] tag Output authentication tag.\n @return The result of the KMAC operation."]
    pub fn otcrypto_kmac(
        key: *mut otcrypto_blinded_key_t,
        input_message: otcrypto_const_byte_buf_t,
        customization_string: otcrypto_const_byte_buf_t,
        required_output_len: usize,
        tag: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Performs KMAC-KDF as specified in NIST SP 800-108r1.\n\n KMAC-KDF can use either KMAC128 or KMAC256; which one is determined by the\n key mode in the configuration of `key_derivation_key`.\n\n Because of limitations on the KMAC hardware, labels longer than 32 bytes are\n not supported.\n\n The caller should allocate and partially populate the `output_key_material`\n blinded key struct, including populating the key configuration and\n allocating space for the keyblob. The configuration may not indicate a\n hardware-backed key and must indicate a symmetric mode. The allocated\n keyblob length for the output key should be twice the unmasked key length\n indicated in its key configuration, rounded up to the nearest 32-bit word.\n The value in the `checksum` field of the blinded key struct will be\n populated by the key derivation function.\n\n @param key_derivation_key Blinded key derivation key.\n @param kmac_mode Either KMAC128 or KMAC256 as PRF.\n @param label Label string (optional, may be empty).\n @param context Context string (optional, may be empty).\n @param[out] output_key_material Blinded output key material.\n @return Result of the key derivation operation."]
    pub fn otcrypto_kmac_kdf(
        key_derivation_key: *mut otcrypto_blinded_key_t,
        label: otcrypto_const_byte_buf_t,
        context: otcrypto_const_byte_buf_t,
        output_key_material: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
pub const otcrypto_rsa_padding_kOtcryptoRsaPaddingPkcs: otcrypto_rsa_padding = 2382;
pub const otcrypto_rsa_padding_kOtcryptoRsaPaddingPss: otcrypto_rsa_padding = 1713;
#[doc = " Enum to define padding scheme for RSA signature data.\n\n Values are hardened."]
pub type otcrypto_rsa_padding = ::core::ffi::c_uint;
#[doc = " Enum to define padding scheme for RSA signature data.\n\n Values are hardened."]
pub use self::otcrypto_rsa_padding as otcrypto_rsa_padding_t;
pub const otcrypto_rsa_size_kOtcryptoRsaSize2048: otcrypto_rsa_size = 1489;
pub const otcrypto_rsa_size_kOtcryptoRsaSize3072: otcrypto_rsa_size = 3125;
pub const otcrypto_rsa_size_kOtcryptoRsaSize4096: otcrypto_rsa_size = 2266;
#[doc = " Enum to define possible lengths of RSA (public) keys.\n\n Values are hardened."]
pub type otcrypto_rsa_size = ::core::ffi::c_uint;
#[doc = " Enum to define possible lengths of RSA (public) keys.\n\n Values are hardened."]
pub use self::otcrypto_rsa_size as otcrypto_rsa_size_t;
unsafe extern "C" {
    #[doc = " Performs the RSA key generation.\n\n Computes RSA private key (d) and the public key modulus (n).\n\n The caller should allocate space for the public key and set the `key` and\n `key_length` fields accordingly.\n\n The caller should fully populate the blinded key configuration and allocate\n space for the keyblob, setting `config.key_length` and `keyblob_length`\n accordingly.\n\n The value in the `checksum` field of key structs is not checked here and\n will be populated by the key generation function.\n\n @param size RSA size parameter.\n @param[out] public_key Pointer to public key struct.\n @param[out] private_key Pointer to blinded private key struct.\n @return Result of the RSA key generation."]
    pub fn otcrypto_rsa_keygen(
        size: otcrypto_rsa_size_t,
        public_key: *mut otcrypto_unblinded_key_t,
        private_key: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Constructs an RSA public key from the modulus and public exponent.\n\n The caller should allocate space for the public key and set the `key` and\n `key_length` fields accordingly. The public exponent is implicitly fixed\n to e=2^16+1.\n\n @param size RSA size parameter.\n @param modulus RSA modulus (n).\n @param exponent RSA public exponent (e).\n @param[out] public_key Destination public key struct.\n @return Result of the RSA key construction."]
    pub fn otcrypto_rsa_public_key_construct(
        size: otcrypto_rsa_size_t,
        modulus: otcrypto_const_word32_buf_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Constructs an RSA private key from the modulus and public/private exponents.\n\n The caller should allocate space for the private key and set the `keyblob`,\n `keyblob_length`, and `key_length` fields accordingly.\n\n @param size RSA size parameter.\n @param modulus RSA modulus (n).\n @param d_share0 First share of the RSA private exponent d.\n @param d_share1 Second share of the RSA private exponent d.\n @param[out] public_key Destination public key struct.\n @return Result of the RSA key construction."]
    pub fn otcrypto_rsa_private_key_from_exponents(
        size: otcrypto_rsa_size_t,
        modulus: otcrypto_const_word32_buf_t,
        d_share0: otcrypto_const_word32_buf_t,
        d_share1: otcrypto_const_word32_buf_t,
        private_key: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Constructs an RSA keypair from the public key and one prime cofactor.\n\n The caller should allocate space for the private key and set the `keyblob`,\n `keyblob_length`, and `key_length` fields accordingly. Similarly, the caller\n should allocate space for the public key and set the `key` and `key_length`\n fields.\n\n @param size RSA size parameter.\n @param modulus RSA modulus (n).\n @param cofactor_share0 First share of the prime cofactor (p or q).\n @param cofactor_share1 Second share of the prime cofactor (p or q).\n @param[out] public_key Destination public key struct.\n @param[out] private_key Destination private key struct.\n @return Result of the RSA key construction."]
    pub fn otcrypto_rsa_keypair_from_cofactor(
        size: otcrypto_rsa_size_t,
        modulus: otcrypto_const_word32_buf_t,
        cofactor_share0: otcrypto_const_word32_buf_t,
        cofactor_share1: otcrypto_const_word32_buf_t,
        public_key: *mut otcrypto_unblinded_key_t,
        private_key: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Computes the digital signature on the input message data.\n\n The caller should allocate space for the `signature` buffer\n and set the length of expected output in the `len` field of\n `signature`. If the user-set length and the output length does not\n match, an error message will be returned.\n\n @param private_key Pointer to blinded private key struct.\n @param message_digest Message digest to be signed (pre-hashed).\n @param padding_mode Padding scheme to be used for the data.\n @param[out] signature Pointer to the generated signature struct.\n @return The result of the RSA signature generation."]
    pub fn otcrypto_rsa_sign(
        private_key: *const otcrypto_blinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        padding_mode: otcrypto_rsa_padding_t,
        signature: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Verifies the authenticity of the input signature.\n\n An \"OK\" status code does not mean that the signature passed verification;\n the caller must check both the returned status and `verification_result`\n before trusting the signature.\n\n @param public_key Pointer to public key struct.\n @param message_digest Message digest to be verified (pre-hashed).\n @param padding_mode Padding scheme to be used for the data.\n @param signature Pointer to the input signature to be verified.\n @param[out] verification_result Result of signature verification.\n @return Result of the RSA verify operation."]
    pub fn otcrypto_rsa_verify(
        public_key: *const otcrypto_unblinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        padding_mode: otcrypto_rsa_padding_t,
        signature: otcrypto_const_word32_buf_t,
        verification_result: *mut hardened_bool_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Encrypts a message with RSA.\n\n The only padding scheme available is OAEP, where the hash function is a\n member of the SHA-2 or SHA-3 family and the mask generation function is\n MGF1 with the same hash function.\n\n OAEP imposes strict limits on the length of the message (see IETF RFC 8017\n for details). Specifically, the message is at most k - 2*hLen - 2 bytes\n long, where k is the byte-length of the RSA modulus and hLen is the length\n of the hash function digest. If the message is too long, this function will\n return an error.\n\n The caller should allocate space for the `ciphertext` buffer and set the\n length of expected output in the `len` field of `signature`. The ciphertext\n is always the same length as the RSA modulus (so an RSA-2048 ciphertext is\n always 2048 bits long). If the length does not match the private key mode,\n this function returns an error.\n\n Note: RSA encryption is included for compatibility with legacy interfaces,\n and is typically not recommended for modern applications because it is\n slower and more fragile than other encryption methods. Consult an expert\n before using RSA encryption.\n\n @param private_key Pointer to public key struct.\n @param hash_mode Hash function to use for OAEP encoding.\n @param message Message to encrypt.\n @param label Label for OAEP encoding.\n @param[out] ciphertext Buffer for the ciphertext.\n @return The result of the RSA encryption operation."]
    pub fn otcrypto_rsa_encrypt(
        public_key: *const otcrypto_unblinded_key_t,
        hash_mode: otcrypto_hash_mode_t,
        message: otcrypto_const_byte_buf_t,
        label: otcrypto_const_byte_buf_t,
        ciphertext: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Decrypts a message with RSA.\n\n The only padding scheme available is OAEP, where the hash function is a\n member of the SHA-2 or SHA-3 family and the mask generation function is\n MGF1 with the same hash function.\n\n The caller should allocate space for the `plaintext` buffer and set the\n allocated length in the `len` field. The length should be at least as long\n as the maximum message length imposed by OAEP; that is, k - 2*hLen - 2 bytes\n long, where k is the byte-length of the RSA modulus and hLen is the length\n of the hash function digest. If the plaintext buffer is not long enough,\n this function will return an error.\n\n Decryption recovers the original length of the plaintext buffer and will\n return its value in `plaintext_bytelen`.\n\n Note: RSA encryption is included for compatibility with legacy interfaces,\n and is typically not recommended for modern applications because it is\n slower and more fragile than other encryption methods. Consult an expert\n before using RSA encryption.\n\n @param private_key Pointer to blinded private key struct.\n @param hash_mode Hash function to use for OAEP encoding.\n @param ciphertext Ciphertext to decrypt.\n @param label Label for OAEP encoding.\n @param[out] plaintext Buffer for the decrypted message.\n @param[out] plaintext_bytelen Recovered byte-length of plaintext.\n @return Result of the RSA decryption operation."]
    pub fn otcrypto_rsa_decrypt(
        private_key: *const otcrypto_blinded_key_t,
        hash_mode: otcrypto_hash_mode_t,
        ciphertext: otcrypto_const_word32_buf_t,
        label: otcrypto_const_byte_buf_t,
        plaintext: otcrypto_byte_buf_t,
        plaintext_bytelen: *mut usize,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts the asynchronous RSA key generation function.\n\n Initializes OTBN and starts the OTBN routine to compute the RSA\n private key (d), RSA public key exponent (e) and modulus (n).\n\n @param size RSA size parameter.\n @return Result of async RSA keygen start operation."]
    pub fn otcrypto_rsa_keygen_async_start(size: otcrypto_rsa_size_t) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes the asynchronous RSA key generation function.\n\n See `otcrypto_rsa_keygen` for details on the requirements for `public_key`\n and `private_key`.\n\n @param[out] public_key Pointer to public key struct.\n @param[out] private_key Pointer to blinded private key struct.\n @return Result of asynchronous RSA keygen finalize operation."]
    pub fn otcrypto_rsa_keygen_async_finalize(
        public_key: *mut otcrypto_unblinded_key_t,
        private_key: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts constructing an RSA private key using a cofactor.\n\n See `otcrypto_rsa_keypair_from_cofactor` for the details\n on the requirements for input buffers.\n\n @param size RSA size parameter.\n @param modulus RSA modulus (n).\n @param cofactor_share0 First share of the prime cofactor (p or q).\n @param cofactor_share1 Second share of the prime cofactor (p or q).\n @return Result of the RSA key construction."]
    pub fn otcrypto_rsa_keypair_from_cofactor_async_start(
        size: otcrypto_rsa_size_t,
        modulus: otcrypto_const_word32_buf_t,
        cofactor_share0: otcrypto_const_word32_buf_t,
        cofactor_share1: otcrypto_const_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes constructing an RSA private key using a cofactor.\n\n See `otcrypto_rsa_keypair_from_cofactor` for the details\n on the requirements for output buffers.\n\n The public key returned from this function is recomputed; we recommend that\n the caller compare it to the originally passed public key value to ensure\n that everything went as expected. If the key is invalid in certain ways\n (such as the modulus not being divisible by the key), then the modulus will\n not match the original input.\n\n @param[out] public_key Destination public key struct.\n @param[out] private_key Destination private key struct.\n @return Result of the RSA key construction."]
    pub fn otcrypto_rsa_keypair_from_cofactor_async_finalize(
        public_key: *mut otcrypto_unblinded_key_t,
        private_key: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts the asynchronous digital signature generation function.\n\n Initializes OTBN and starts the OTBN routine to compute the digital\n signature on the input message.\n\n @param private_key Pointer to blinded private key struct.\n @param message_digest Message digest to be signed (pre-hashed).\n @param padding_mode Padding scheme to be used for the data.\n @return Result of async RSA sign start operation."]
    pub fn otcrypto_rsa_sign_async_start(
        private_key: *const otcrypto_blinded_key_t,
        message_digest: otcrypto_hash_digest_t,
        padding_mode: otcrypto_rsa_padding_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes the asynchronous digital signature generation function.\n\n See `otcrypto_rsa_sign` for details on the requirements for `signature`.\n\n @param[out] signature Pointer to generated signature struct.\n @return Result of async RSA sign finalize operation."]
    pub fn otcrypto_rsa_sign_async_finalize(signature: otcrypto_word32_buf_t) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts the asynchronous signature verification function.\n\n Initializes OTBN and starts the OTBN routine to recover the message\n from the input signature.\n\n @param public_key Pointer to public key struct.\n @param signature Pointer to the input signature to be verified.\n @return Result of async RSA verify start operation."]
    pub fn otcrypto_rsa_verify_async_start(
        public_key: *const otcrypto_unblinded_key_t,
        signature: otcrypto_const_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes the asynchronous signature verification function.\n\n An \"OK\" status code does not mean that the signature passed verification;\n the caller must check both the returned status and `verification_result`\n before trusting the signature.\n\n @param message_digest Message digest to be verified (pre-hashed).\n @param padding_mode Padding scheme to be used for the data.\n @param[out] verification_result Result of signature verification.\n @return Result of async RSA verify finalize operation."]
    pub fn otcrypto_rsa_verify_async_finalize(
        message_digest: otcrypto_hash_digest_t,
        padding_mode: otcrypto_rsa_padding_t,
        verification_result: *mut hardened_bool_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts the asynchronous encryption function.\n\n See `otcrypto_rsa_encrypt` for details on the length requirements for\n `message`.\n\n @param private_key Pointer to public key struct.\n @param hash_mode Hash function to use for OAEP encoding.\n @param message Message to encrypt.\n @param label Label for OAEP encoding.\n @return The result of the RSA encryption start operation."]
    pub fn otcrypto_rsa_encrypt_async_start(
        public_key: *const otcrypto_unblinded_key_t,
        hash_mode: otcrypto_hash_mode_t,
        message: otcrypto_const_byte_buf_t,
        label: otcrypto_const_byte_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes the asynchronous encryption function.\n\n See `otcrypto_rsa_encrypt` for details on the length requirements for\n `ciphertext`. Infers the RSA size from `ciphertext`'s length, and will\n return an error if this does not match the RSA size for the current OTBN\n data.\n\n @param[out] ciphertext Buffer for the ciphertext.\n @return The result of the RSA encryption operation."]
    pub fn otcrypto_rsa_encrypt_async_finalize(
        ciphertext: otcrypto_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts the asynchronous decryption function.\n\n See `otcrypto_rsa_decrypt` for details on the length requirements for\n `ciphertext`.\n\n @param private_key Pointer to blinded private key struct.\n @param ciphertext Ciphertext to decrypt.\n @return Result of the RSA decryption start operation."]
    pub fn otcrypto_rsa_decrypt_async_start(
        private_key: *const otcrypto_blinded_key_t,
        ciphertext: otcrypto_const_word32_buf_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes the asynchronous decryption function.\n\n See `otcrypto_rsa_decrypt` for details on the requirements for `plaintext`\n length and the way in which the actual length of the plaintext is returned.\n\n @param hash_mode Hash function to use for OAEP encoding.\n @param label Label for OAEP encoding.\n @param[out] plaintext Buffer for the decrypted message.\n @param[out] plaintext_bytelen Recovered byte-length of plaintext.\n @return Result of the RSA decryption finalize operation."]
    pub fn otcrypto_rsa_decrypt_async_finalize(
        hash_mode: otcrypto_hash_mode_t,
        label: otcrypto_const_byte_buf_t,
        plaintext: otcrypto_byte_buf_t,
        plaintext_bytelen: *mut usize,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " One-shot SHA3-224 hash computation.\n\n The caller should allocate space for the digest and set `digest.len`\n accordingly. The function will return an error if the length is not 224\n bits (= 7 32-bit words). The `digest.mode` field is set by this function and\n may be uninitialized.\n\n @param message Input message.\n @param[out] digest Computed digest.\n @return OK or error."]
    pub fn otcrypto_sha3_224(
        message: otcrypto_const_byte_buf_t,
        digest: *mut otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " One-shot SHA3-256 hash computation.\n\n The caller should allocate space for the digest and set `digest.len`\n accordingly. The function will return an error if the length is not 256\n bits (= 8 32-bit words). The `digest.mode` field is set by this function and\n may be uninitialized.\n\n @param message Input message.\n @param[out] digest Computed digest.\n @return OK or error."]
    pub fn otcrypto_sha3_256(
        message: otcrypto_const_byte_buf_t,
        digest: *mut otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " One-shot SHA3-384 hash computation.\n\n The caller should allocate space for the digest and set `digest.len`\n accordingly. The function will return an error if the length is not 384\n bits (= 12 32-bit words). The `digest.mode` field is set by this function and\n may be uninitialized.\n\n @param message Input message.\n @param[out] digest Computed digest.\n @return OK or error."]
    pub fn otcrypto_sha3_384(
        message: otcrypto_const_byte_buf_t,
        digest: *mut otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " One-shot SHA3-512 hash computation.\n\n The caller should allocate space for the digest and set `digest.len`\n accordingly. The function will return an error if the length is not 512\n bits (= 16 32-bit words). The `digest.mode` field is set by this function and\n may be uninitialized.\n\n @param message Input message.\n @param[out] digest Computed digest.\n @return OK or error."]
    pub fn otcrypto_sha3_512(
        message: otcrypto_const_byte_buf_t,
        digest: *mut otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " One-shot SHAKE128 hash computation.\n\n The caller should allocate space for the digest and set `digest.len`\n according to their desired output length. The `digest.mode` field is set by\n this function and may be uninitialized.\n\n @param message Input message.\n @param[out] digest Computed digest.\n @return OK or error."]
    pub fn otcrypto_shake128(
        message: otcrypto_const_byte_buf_t,
        digest: *mut otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " One-shot SHAKE256 hash computation.\n\n The caller should allocate space for the digest and set `digest.len`\n according to their desired output length. The `digest.mode` field is set by\n this function and may be uninitialized.\n\n @param message Input message.\n @param[out] digest Computed digest.\n @return OK or error."]
    pub fn otcrypto_shake256(
        message: otcrypto_const_byte_buf_t,
        digest: *mut otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " One-shot cSHAKE128 hash computation.\n\n The caller should allocate space for the digest and set `digest.len`\n according to their desired output length. The `digest.mode` field is set by\n this function and may be uninitialized.\n\n The function name and customization string parameters are defined in NIST\n SP800-185; please refer to that document for guidance on their usage.\n\n @param message Input message.\n @param function_name_string Function name parameter (may be empty).\n @param customization_string Customization parameter (may be empty).\n @param[out] digest Computed digest.\n @return OK or error."]
    pub fn otcrypto_cshake128(
        message: otcrypto_const_byte_buf_t,
        function_name_string: otcrypto_const_byte_buf_t,
        customization_string: otcrypto_const_byte_buf_t,
        digest: *mut otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " One-shot cSHAKE256 hash computation.\n\n The caller should allocate space for the digest and set `digest.len`\n according to their desired output length. The `digest.mode` field is set by\n this function and may be uninitialized.\n\n The function name and customization string parameters are defined in NIST\n SP800-185; please refer to that document for guidance on their usage.\n\n @param message Input message.\n @param function_name_string Function name parameter (may be empty).\n @param customization_string Customization parameter (may be empty).\n @param[out] digest Computed digest.\n @return OK or error."]
    pub fn otcrypto_cshake256(
        message: otcrypto_const_byte_buf_t,
        function_name_string: otcrypto_const_byte_buf_t,
        customization_string: otcrypto_const_byte_buf_t,
        digest: *mut otcrypto_hash_digest_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Generates a key pair for X25519.\n\n The caller should allocate and partially populate the blinded key struct,\n including populating the key configuration and allocating space for the\n keyblob. For a hardware-backed key, use the private key handle returned by\n `otcrypto_hw_backed_key`. Otherwise, the mode should indicate X25519 and the\n keyblob should be 80 bytes. The value in the `checksum` field of the blinded\n key struct will be populated by the key generation function.\n\n @param[out] private_key Pointer to the blinded private key struct.\n @param[out] public_key Pointer to the unblinded public key struct.\n @return Result of the X25519 key generation."]
    pub fn otcrypto_x25519_keygen(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Elliptic-curve Diffie Hellman shared secret generation with Curve25519.\n\n @param private_key Pointer to blinded private key (u-coordinate).\n @param public_key Pointer to the public scalar from the sender.\n @param[out] shared_secret Pointer to shared secret key (u-coordinate).\n @return Result of the X25519 operation."]
    pub fn otcrypto_x25519(
        private_key: *const otcrypto_blinded_key_t,
        public_key: *const otcrypto_unblinded_key_t,
        shared_secret: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous key generation for X25519.\n\n See `otcrypto_x25519_keygen` for requirements on input values.\n\n @param private_key Destination structure for private key, or key handle.\n @return Result of asynchronous X25519 keygen start operation."]
    pub fn otcrypto_x25519_keygen_async_start(
        private_key: *const otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes the asynchronous key generation for X25519.\n\n See `otcrypto_x25519_keygen` for requirements on input values.\n\n May block until the operation is complete.\n\n The caller should ensure that the private key configuration matches that\n passed to the `_start` function.\n\n @param[out] private_key Pointer to the blinded private key struct.\n @param[out] public_key Pointer to the unblinded public key struct.\n @return Result of asynchronous ECDSA keygen finalize operation."]
    pub fn otcrypto_x25519_keygen_async_finalize(
        private_key: *mut otcrypto_blinded_key_t,
        public_key: *mut otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Starts asynchronous shared secret generation for X25519.\n\n See `otcrypto_x25519` for requirements on input values.\n\n @param private_key Pointer to the blinded private key (u-coordinate).\n @param public_key Pointer to the public scalar from the sender.\n @return Result of the async X25519 start operation."]
    pub fn otcrypto_x25519_async_start(
        private_key: *const otcrypto_blinded_key_t,
        public_key: *const otcrypto_unblinded_key_t,
    ) -> otcrypto_status_t;
}
unsafe extern "C" {
    #[doc = " Finalizes asynchronous shared secret generation for X25519.\n\n See `otcrypto_x25519` for requirements on input values.\n\n May block until the operation is complete.\n\n @param[out] shared_secret Pointer to shared secret key (u-coordinate).\n @return Result of async X25519 finalize operation."]
    pub fn otcrypto_x25519_async_finalize(
        shared_secret: *mut otcrypto_blinded_key_t,
    ) -> otcrypto_status_t;
}
